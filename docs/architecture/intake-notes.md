# Заметки по разбору (intake) — что Юрий объясняет по ходу

> Режим: СОБИРАЕМ требования. НЕ реализуем, пока Юрий не скажет «стоп, делаем».
> Итог потом переносим в SYSTEM-MAP.md и/или в спеки.

---

## Раздел: Настройки найма → Дефолты компании → Основные

### 1. «Выбор компании в анкете вакансии» (тумблер + мультикомпанийность)

Назначение: для рекрутинговых компаний / найма под клиентов (аутсорсинг/рекрутинг) —
одна учётка ведёт найм под несколько разных компаний-клиентов.

Как должно работать:
- В настройках — СПИСОК компаний:
  - **Компания №1 (основная)** — по умолчанию, данные берутся из настроек компании:
    название, слоган, **логотип** (нужен для вакансий), описание.
  - Кнопка **«+ Добавить ещё одну компанию»** → вторая, третья… **до ~30** (пример).
  - У каждой компании в списке свои: название, слоган, логотип, описание.
- При создании вакансии (когда составляем анкету) — HR **выбирает**, под какую из
  этих компаний идёт вакансия.
- Тумблер «Выбор компании в анкете вакансии»:
  - **ВКЛ** (несколько компаний) → в анкете показывается блок выбора компании
    («можете выбрать»).
  - **ВЫКЛ** → в анкете выбор НЕ показывается.

❓ Уточнить на этапе анкеты: при включённом тумблере в анкете кандидат сам ВЫБИРАЕТ
компанию, или просто ВИДИТ, под какую компанию вакансия? (Юрий покажет экран анкеты позже.)

Связанные экраны: 01, 02 (Дефолты компании → Основные).

### 2. «Автоматический сбор обратной связи» → перенести в САМУ вакансию

Сейчас: блок в дефолтах компании (статус «Скоро»). Опросы новых сотрудников на
контрольных точках адаптации: 30 дн «Как проходит адаптация?», 60 дн «Чувствуете
ли уверенность?», 90 дн «Оправдались ли ожидания?».

Решение Юрия: это **не место для дефолтов компании** — должно быть **в каждой
вакансии своё** (у каждой вакансии свои опросы адаптации). Перенести настройку
из «Дефолты компании» в настройки вакансии.

Связанные экраны: 03 (верхний блок).

### 3. «Хранение данных кандидатов» (ФЗ-152) — предупреждение при увеличении срока

Сейчас: дропдаун срока авто-удаления ПДн отказанных кандидатов, по умолчанию **6 месяцев**.

Требования Юрия:
- По умолчанию у всех — 6 месяцев.
- Если HR меняет на **больший** срок → показать **предупреждение о нарушении
  законодательства** (ФЗ-152 требует удалять раньше / не хранить дольше нужного).
- Если HR **сознательно** проигнорировал предупреждение (нажал «всё равно сохранить»)
  → **записать в лог** (где-то в аудит/журнал: кто, когда, какой срок поставил).

Связанные экраны: 03, 04 (блок «Хранение данных кандидатов»).

---

## СКВОЗНЫЕ ТРЕБОВАНИЯ (ко всем настройкам платформы)

### S1. Настройки ДОЛЖНЫ сохраняться и переживать перезагрузку

Везде, где есть настройки (этот раздел и все остальные): после нажатия «Сохранить»
значение должно реально сохраниться и **остаться после перезагрузки страницы** —
а НЕ сбрасываться обратно. Юрий явно отметил, что местами настройка после reload
почему-то сбрасывается. ⚠️ Проверить персистентность ВСЕХ настроек при разборе
(частый баг: дефолтное состояние перетирает сохранённое при загрузке формы).

---

## Раздел: Дефолты компании → Webhooks + Битрикс24 (куда отнести)

### 4. Интеграция с Битрикс24 — разнести на два уровня (рекомендация Claude)

Юрий сомневается, где место: дефолты компании или вакансия. Контекст от Юрия:
подключаем Битрикс один раз, но у каждой вакансии может быть своя воронка Битрикса —
её надо выбрать и прицепить к этой вакансии.

**Рекомендация (по образцу hh.ru — экран 07):**
- **Подключение Битрикс24 → уровень компании** (таб «Интеграции»): webhook URL,
  креды, «Проверить связь». Один раз на компанию.
- **Выбор воронки Битрикса + «когда отправлять» + вкл/выкл → в карточке вакансии.**
  По каждой вакансии своя воронка.
- Обоснование: на экране 07 про hh.ru написано «здесь только подключение площадок,
  управление — в карточке каждой вакансии». Делаем Битрикс по тому же паттерну =
  единообразно, креды не дублируются, воронка гибко на вакансию.
- ⚙️ Потребует: чтение списка воронок из Битрикса по API для селекта в вакансии.

**Webhooks (generic, события):** оставить на уровне компании (один эндпоинт на оргу,
это dev-интеграция). Переопределение на вакансию — потом, если понадобится.

❓ Ждёт финального решения Юрия (он «не уверен до конца»).

Связанные экраны: 03, 04 (Webhooks, Битрикс24), 07 (hh.ru как образец паттерна).

---

## Раздел: Дефолты компании → Интеграции (таб)

### 5. UI-баги карточки hh.ru (починить)
- Кнопка «Отключить» вылезает за рамку карточки.
- Блок имя/даты (Штумпф Юрий Геннадьевич · Подключён 28 апр · Синхр 3 июня)
  свёрстан криво — выровнять нормально.
Связанные экраны: 07.

### 6. IA: «Интеграции» вынести из «Дефолты компании»
Подключение площадок — НЕ «дефолт, применяемый к новым вакансиям». Семантически
таб «Интеграции» не на месте. Предложение: вынести подключения площадок/CRM в
отдельный раздел настроек (не таб дефолтов вакансии).

### 7. hh-подключение: поддержать НЕСКОЛЬКО аккаунтов на тенант  ⚠️ важно
Юрий: сценарий «до 30 компаний» бывает **обоих** видов:
- (а) одно агентство, ОДИН аккаунт hh, 30 компаний — бренды под ним;
- (б) 30 разных работодателей, у каждого СВОЙ аккаунт hh.
Значит нужно: **N hh-подключений на тенант** (0..N) + привязка «компания(бренд)/
вакансия → конкретное hh-подключение». При создании вакансии под компанию X
используется hh-подключение, привязанное к X.
Тех.деталь: hh OAuth-токен привязан к работодателю на hh — за чужой аккаунт
подключить нельзя, каждый отдельный работодатель логинится сам.
⚠️ Это изменение схемы: сейчас один токен на company_id (тенант). Нужна таблица
hh-подключений с id + связь с компанией-брендом. Большая задача, спланировать отдельно.

### 8. CRM-системы — паттерн как у Битрикса (рекомендация)
Подключать ОДИН раз на уровне аккаунта (раздел «Интеграции»), а выбирать/прицеплять
точечно: в вакансии (найм) или в будущем разделе «Продажи». Не «одна намертво на
всех» — подключил, дальше выбираешь, где использовать.
Связанные экраны: 07.

### 9. «Другие площадки» (Авито/SuperJob/Яндекс, все «Скоро») — скрыть от клиентов
Юрий: пока не уверен, нужны ли вообще. Клиентам эти «Скоро»-заглушки лучше НЕ
показывать. Оставить видимыми только на уровне администратора платформы (как
roadmap «скоро»), а в клиентском UI — скрыть.

❓ Возможный общий принцип (уточнить): прятать ВСЕ «Скоро»-заглушки от клиентов и
показывать только платформенному админу? (такие блоки есть и в др. местах:
авто-сбор обратной связи, интеграции календаря, поддомен — тоже «Скоро»).
Связанные экраны: 07.

---

## Раздел: Расписание (таб) — экраны 05, 06 + новые

### 10. Ссылка «Брендинг настраивается в Настройках компании»
Юрий: брендинг скорее относится туда, где компании. Но сомневается, нужна ли
вообще эта подсказка/ссылка тут. ❓ Под вопросом — оставить/убрать.

### 11. «Способы проведения интервью» — новый список
Сейчас: Zoom, Google Meet, Telegram, Звонок по телефону, Встреча в офисе.
Нужно (список Юрия):
- Видео-звонок Zoom
- Видео-звонок **Я.Телемост** (Яндекс Телемост) — НОВЫЙ
- Видео-звонок Google Meet
- Звонок по телефону
- Встреча в офисе
⚠️ В списке Юрия НЕТ Telegram → видимо заменить Telegram на Я.Телемост.
❓ Подтвердить: Telegram убираем совсем или оставляем + добавляем Я.Телемост?

### 12. «Интеграции календарей» — список
Нужно: Google Календарь, Яндекс Календарь, + «все по ссылке, которые можно
подключить» (вероятно подключение календаря по ICS/URL-ссылке). Статус сейчас «Скоро».

### 13. «Слоты для интервью» — дефолты
- Длительность по умолчанию: **45 минут** (сейчас 30).
- Буфер между интервью: 15 минут (как есть).

### 14. «Напоминания» — добавить текст/инструкции
К напоминаниям об интервью нужен настраиваемый текст:
- если встреча в офисе → **адрес** (куда идти);
- если Zoom/видео → **ссылка + инструкция**.
Отдельный текст для «за 24 часа» и отдельный для «за 2 часа» (разные сообщения).

---

## Раздел: Воронка (таб) — экран «Воронка»

Дефолтный сценарий «Стандартный»: 1 Новый отклик → 2 Скрининг → 3 Демонстрация →
4 Интервью с HR → 5 Интервью с руководителем → 6 Оффер → 7 Выход на работу.
+ Автоматизация (3 тумблера: авто-демо после отклика, авто-интервью после демо
при скоре>N, авто-отклонение при стоп-факторе).

### 15. Воронка должна НАСТРАИВАТЬСЯ (стадии редактируемые)
Юрий: воронку нужно настраивать. ℹ️ В коде уже есть **Funnel Builder (Group 22**,
флаг `vacancy.funnelBuilderEnabled`, 17 типов блоков, drag-and-drop) — вероятно
переиспользовать его, а не делать заново. Проверить при разборе, как связать
«Дефолтный сценарий воронки» здесь с Funnel Builder.

### 16. Сопоставление воронки платформы ↔ hh.ru  ⚠️ важно
Нужно: маппинг стадий «воронка платформы ↔ воронка на hh». Когда кандидат
откликнулся на hh — он должен попадать в соответствующую стадию платформенной
воронки (и обратно — смена стадии на платформе отражается в стадии hh).
ℹ️ Уже начато в коде: read-only эндпоинт стадий hh (`hh-stages`), перевод отклика
в стадию hh «Тестовое задание» при отправке теста (коммиты 4820664b, 6ae093b4).
Нужно достроить: **UI сопоставления стадий**.
❓ Место настройки под вопросом — может здесь, может в другом разделе. Юрий
уточнит позже («дойдём до другого места, я там скажу, что может быть здесь»).

---

## Раздел: Сообщения (таб) — экраны «Сообщения»

Блоки на табе: Шаблоны сообщений (Скоро), Режим строгости общения (Строго/Мягко),
Безопасность отправки сообщений (мин. задержка 31 сек), Корзина — срок хранения (30 дн).

### 17. «Шаблоны сообщений» + «Режим строгости общения» — возможно НЕ нужны здесь
Юрий: кажется, этих двух блоков тут не надо; посмотрит позже по ходу разбора,
может удалить отсюда (вроде бы со всеми настройками уже понятно / настраивается
в другом месте). ❓ TBD — решит позже.
ℹ️ Контекст: «Режим строгости общения» = чувствительность pre-filter AI-чат-бота
к abuse (Group 22: soft/moderate/strict). Проверить, не дублирует ли настройку в
другом месте.

### 18. «Безопасность отправки сообщений» + «Корзина — срок хранения» — перенести
Юрий: эти два блока — служебные/технические, им место НЕ здесь, а там, где
системные настройки (рядом с «сколько хранятся кандидаты» / ФЗ-152 из таба
Основные). Вариант: скомпоновать вместе или сделать отдельный таб «Служебные/
Системные настройки».
ℹ️ Контекст: «Безопасность отправки» = мин. задержка между авто-сообщениями в
hh-чат (анти-блокировка hh, реком. 31 сек / мин. 21). «Корзина — срок хранения» =
`companies.trash_retention_days` (дефолт 30).
→ Связано с п.3 (хранение данных ФЗ-152) — всё это в одну группу «служебных настроек».

---

## Раздел: Стоп-факторы (таб) — экран «Стоп-факторы»

Список стоп-факторов резюме (тумблеры): Город/релокация, Формат работы
(офис/гибрид/удалёнка), Возраст, Минимальный опыт, Обязательные документы
(вод.права/мед.книжка), Гражданство, Макс. зарплатные ожидания. Подсказка: это
ДЕФОЛТЫ компании, реальные стоп-факторы — на каждой вакансии (таб «Воронка» →
«Стоп-факторы по резюме»). + тумблер «Применять стоп-факторы автоматически при
создании вакансии» + «HR может вручную вернуть отклонённого кандидата».
ℹ️ В коде: `vacancy.stopFactorsJson`, применяются в lib/hh/process-queue.ts.

### 19. Блок ок по сути, но тоже вынести из вложенных табов
Юрий: сам блок норм, но его (как и многое выше) стоит вынести из этих вложенных
табов. Просит предложить общую структуру — «как лучше ты это видишь».

---

## 🧭 ПРЕДЛОЖЕНИЕ ПО ОБЩЕЙ СТРУКТУРЕ НАСТРОЕК (рекомендация Claude)

Сейчас всё свалено в «Дефолты компании» с табами (Основные/Интеграции) + второй
набор табов (Расписание/Воронка/Сообщения/Стоп-факторы), смешаны разные по смыслу
вещи. Предлагаю сгруппировать по НАЗНАЧЕНИЮ — 4 раздела:

1. **Профиль / Компании** (бизнес/брендинг)
   - Название, слоган, логотип, описание.
   - Список компаний (мультикомпанийность, до 30) + тумблер «выбор компании в анкете».

2. **Интеграции** (подключения — отдельный раздел, НЕ таб дефолтов)
   - hh.ru (несколько подключений), CRM/Битрикс24 (подключение), календари.
   - Подключаем здесь; ВЫБОР воронки/использование — в вакансии.

3. **Дефолты вакансии** (шаблон для новых вакансий, в каждой переопределяется)
   - Воронка (сценарий + Funnel Builder), Расписание (способы интервью, слоты,
     напоминания), Стоп-факторы по резюме.
   - Везде плашка «применяется к новым вакансиям, в каждой можно изменить».

4. **Служебные / Системные** (технические)
   - Хранение ПДн (ФЗ-152), Корзина (срок), Темп отправки в hh-чат, Режим строгости AI.
   - Авто-сбор обратной связи (если оставляем на уровне компании — но Юрий хотел в вакансию, п.2).

+ «Скоро»-заглушки — прятать от клиентов, показывать только платформенному админу (п.9).

❓ Это черновик IA — финализируем на этапе планирования. Юрий, посмотри, ок ли
такое деление на 4 раздела.

---

## Раздел: Основные → «Аварийное отключение AI» (экран 01)

### 20. Развернуть рубильник: всё сразу ИЛИ точечно по вакансии
Юрий: хочет, чтобы можно было не только вырубить AI у ВСЕХ вакансий разом, но и
срочно остановить КОНКРЕТНУЮ вакансию. Спрашивает, как лучше.

**Рекомендация Claude:** сделать блок раскрывающимся:
- Сверху — большой «рубильник» «Выключить AI у ВСЕХ вакансий» (как сейчас,
  `companies.aiChatbotKilled` — перекрывает всё). Это аварийная «красная кнопка».
- Кнопка «Показать вакансии» → раскрывается список активных вакансий с включённым
  AI + тумблер у каждой для ТОЧЕЧНОЙ остановки.
- Так в аварии можно либо вырубить всё, либо хирургически одну вакансию, не лазая
  в настройки каждой.

ℹ️ В коде УЖЕ есть нужные рычаги — это в основном про UI (вывести их в одном месте):
- per-vacancy: `vacancy.aiChatbotEnabled`
- per-company (всё сразу): `companies.aiChatbotKilled`
- platform-wide: emergency-эндпоинт kill-all-ai-chatbots
- список активных AI-вакансий уже есть в Platform Admin → «AI Vacancies» (Group 14)
  — переиспользовать тот же запрос для списка здесь.

---

## СКВОЗНЫЕ ТРЕБОВАНИЯ (продолжение)

### S2. Единый дизайн и одинаковая ширина во всех табах/разделах настроек
Юрий: ширина контейнера в разных табах разная (в одном такая, в другом другая).
Нужен **единый аккуратный дизайн**: одинаковая max-width контейнера, одинаковые
отступы, карточки, типографика — по всем табам раздела настроек (и в новой
структуре из 4 разделов тоже). Проверить и привести к общему виду при разборе/
реализации.

---

## ✅ РАБОЧИЙ ПЛАН (согласовано — гибрид)

1. **Сейчас (по ходу сбора):** выкатываем только БЕЗОПАСНЫЕ точечные правки, которые
   НЕ переедут и не зависят от будущей структуры. Катим вечерними пачками.
2. **НЕ трогаем сейчас:** перекладку блоков, новый IA (4 раздела), единый дизайн (S2),
   мультикомпанийность, мульти-hh, маппинг воронки ↔ hh. Это переедет — делать рано.
3. **Потом, один связный заход:** добираем intake (ВКЛ. настройки самой вакансии —
   они зеркалят дефолты), фиксируем целевую структуру, делаем перекладку + единый
   дизайн + крупные фичи по плану/фазам (через brainstorming → спека → план).

### Очередь «безопасных точечных правок» (можно делать сейчас)
- [ ] Слот: дефолт длительности 30 → **45 мин** (п.13).
- [ ] Способы интервью: добавить **Я.Телемост** (п.11; уточнить судьбу Telegram).
- [ ] S1: найти и починить настройки, которые сбрасываются после перезагрузки.
- [ ] (кандидат) UI-баги карточки hh (п.5) — но проверить, не переедет ли карточка
      при выносе «Интеграций»; если риск — отложить.

### Большой заход (после сбора всего, по фазам)
Новый IA (4 раздела) · вынос «Интеграции» · мульти-hh + привязка к компании/вакансии ·
мультикомпанийность (до 30) · перенос авто-обратной-связи в вакансию · перенос
«Безопасность отправки»/«Корзина» в служебные · маппинг воронки ↔ hh · настраиваемая
воронка через Funnel Builder · аварийный рубильник с точечной остановкой · единый
дизайн (S2) · прятать «Скоро» от клиентов · напоминания с текстом/адресом.

---

# ЭКРАН: Редактор вакансии (таб «Анкета»)
URL пример: /hr/vacancies/5ae8f734-b468-46fc-88f9-69ed662879ed?tab=anketa
Блоки формы: 1 Компания, 2 Должность, 3 Мотивация, 4 О компании, 5 Обязанности и
требования, 6 Портрет кандидата, 7 Условия, 8 Документы, 9 AI-генерация,
AI-профиль кандидата. Справа — панель «AI-ассистент» (оценка названия, формат,
аналитика зарплат, рынок, время публикации, анализ мотивации, рекомендации AI,
критично/рекомендации/заполнено).

## A. Переименования / UI (часть — безопасные quick-wins)
- 21. **Таб «Анкета» → переименовать в «Вакансия».** (quick-win, но проверить все
  места, где «Анкета» используется как название этого таба/раздела).
- 22. **Блок 1 «Компания» (Своя/Для клиента)** — показывать выбор ТОЛЬКО если в
  настройках HR > 1 компании. Если одна — всегда она, выбор НЕ показывается.
  (зависит от мультикомпанийности, п.1 — делать в большом заходе).
- 23. **«Количество вакантных мест»** — степпер со стрелками вверх/вниз некомпактен.
  Сделать компактнее (число + кнопки +/− рядом). Посмотреть, как лучше.
- 24. **AI-панель «Варианты с высоким откликом»** — большое расстояние между
  пунктами. Если показывается 3 — убрать лишние зазоры (друг под другом, плотно);
  если может быть до 5 — растянуть аккуратно. Без большого вертикального зазора.
- 25. **AI-панель «Формат работы»** — строку «✅ Удалёнка: пул кандидатов в 3-5 раз
  шире. 88к+ вакансий Q1 2026 (+18% за год)» уместить в одну строку.

## B. ⚠️ АУДИТ AI-скрининга — что реально питает скрининг (investigation)
Сквозная путаница: на полях висит бейдж «Нужно для AI-скрининга» (Обязанности,
Требования, Неприемлемо, Портрет кандидата…), НО Юрий подозревает, что реальный
скрининг идёт по блоку «AI-профиль кандидата» (Идеальный кандидат), а не по этим
полям. Нужно ПРОВЕРИТЬ В КОДЕ (lib/ai-screen-candidate.ts и т.д.):
- 26. Какие именно поля вакансии реально передаются в AI-скрининг резюме? Кто
  использует «Обязанности»/«Требования»/«Неприемлемо»/«Желаемые параметры»?
- 27. Бейдж «Нужно для AI-скрининга» — правдив ли он на каждом поле, или вводит в
  заблуждение (поле на самом деле не участвует)? Привести бейджи в соответствие
  реальности.
- 28. «Желаемые параметры» (с весами 1-5) — скорее всего УЖЕ участвуют в скрининге;
  подтвердить.
→ Этот аудит отвечает сразу на несколько вопросов Юрия (п.26-28 + баг 31). Сделать
ПЕРВЫМ при паузе — он read-only и проясняет дизайн.

## C. Баги
- 29. **AI-панель «О компании» дублируется** — описание компании показано и в блоке 4
  формы, и ещё раз в правой AI-панели («О компании … Редактировать в настройках»).
  Зачем второй раз? Убрать дубль / решить, где оно живёт.
- 30. **«Подтянуть из настроек»** (блок 4 Описание компании) — проверить, что текст
  подтягивается ИМЕННО из настроек HR/компании (тот же источник, что и брендинг-
  описание). Связано с п.1 (описание компании в дефолтах).
- 31. **Ложное «Критично: О компании отсутствует»** — описание компании ЕСТЬ
  (заполнено), но AI-панель в «Критично» пишет, что отсутствует. Похоже, проверяет
  НЕ то поле. Починить проверку (смотреть на реальное поле описания).

## D. Открытые вопросы
- 32. **Блок 8 «Документы»** (внутренние, не видны кандидатам, до 20 МБ) — Юрий не
  помнит назначение. Пока оставить, но прояснить смысл/использование при разборе.

## E. Блоки «AI-генерация» (9) и «AI-профиль кандидата»
- **AI-генерация (9):** «Вопросы для скрининга» (список) + «Описание для hh.ru»
  (кнопка «Сгенерировать» → превью от AI с разделами О компании/Обязанности/
  Требования/Условия → Принять/Заново/Редактировать).
- **AI-профиль кандидата** (подпись: «Данные для автоматического скрининга и оценки»):
  - «Идеальный кандидат (в свободной форме)» — «AI будет сравнивать каждое резюме
    с этим описанием».
  - «Жёсткие требования (фильтры отсева)»: Обязательные компетенции (hard skills)
    [AI-скрининг] — «без этих навыков рейтинг ниже 50%, совпадение по доле»;
    «Автоматический отказ — стоп-факторы» [AI-скрининг] — «хотя бы один → рейтинг 0»;
    «Минимальный опыт для AI-фильтра (лет)».
  - «Критерии оценки (влияют на балл)»: Опыт в отрасли / Конкретные навыки /
    Зарплатное соответствие / Опыт управления / Образование — каждый со шкалой
    Критично/Важно/Желательно/Не важно. + «Город и формат — жёсткие фильтры, не балл».
  - Уже есть инлайн-подсказка AI: «Навыков: 9 — слишком много, оставьте 4-6…».

### 🔑 Наблюдение для АУДИТА AI-скрининга (п.26-28) — подтверждает подозрение Юрия
Реальные данные скрининга, похоже, ЖИВУТ ИМЕННО ЗДЕСЬ («AI-профиль кандидата»):
Идеальный кандидат + Обязательные компетенции + стоп-факторы + мин. опыт + Критерии
оценки (веса). А поля выше в «Портрет кандидата» (Обязательные навыки / Неприемлемо)
и «Обязанности/Требования» с бейджем «Нужно для AI-скрининга» — ВЕРОЯТНО ДУБЛИРУЮТ
их или не используются. → Это и есть источник путаницы. При аудите:
- определить, какой из двух наборов реально читает lib/ai-screen-candidate.ts;
- убрать/слить дубль, чтобы был ОДИН источник правды для скрининга;
- бейджи «Нужно для AI-скрининга» оставить только на реально используемых полях.

### 33. Контекстные подсказки в правой панели (per-section)
Юрий: правая панель «AI-ассистент» должна показывать подсказки, релевантные ТЕКУЩЕМУ
разделу формы (напр. в «AI-профиль кандидата» — свои подсказки про hard skills/
стоп-факторы/веса), если уместно. Сейчас набор подсказок, видимо, общий. Сделать
подсказки контекстными к блоку (где это уместно).

## F. Шапка вакансии: «Действия» и «Заполнить из…»
- Меню «Действия»: Дублировать, Экспорт в Excel, Остановить, Возобновить,
  Закрыть в архив, Восстановить, В корзину, Удалить навсегда. (часть пунктов
  неактивна по состоянию — ок).
- Меню «Заполнить из…»: Вставить текст / Из библиотеки / Загрузить файл /
  Импорт с hh.ru. Модалка «Привязать вакансию с hh.ru» (вставить ссылку →
  привязка, данные подтягиваются, платформа начинает получать отклики).
  ℹ️ Связано с п.7 (мульти-hh) и п.16 (маппинг воронки) — привязка вакансии к hh.

### 34. «Заполнить из…»: подтверждён только hh.ru, остальные проверить
Юрий: «Импорт с hh.ru» работает хорошо. Про «Вставить текст», «Из библиотеки»,
«Загрузить файл» — пока НЕ может сказать, что работают хорошо. → Проверить, что
эти три способа реально работают; если нет — доработать или временно скрыть.

---

# ЭКРАН: Таб «Демонстрация» (конструктор демо)
Слева список уроков (Приветствие, О руководителе, Почему сейчас, Кого мы ищем,
Ваша роль, Типичная нагрузка, Стек и инструменты, Деньги, Карьерный рост, Команда
и условия, Кому подходит, Вопросы ч.1, Вопросы ч.2, Видео-визитка) + «+ Урок».
Редактор урока: блоки контента (текст / изображение / видео / аудио / файл /
callout / чеклист / оценка-звезда / запись). Меню «Создать из…»: Создать с нуля /
Из библиотеки.

### 35. В конце демонстрации — СИСТЕМНАЯ АНКЕТА, пишущая данные в кандидата
Юрий: в конструкторе создаётся демонстрация (уроки), а В КОНЦЕ должна идти
**системная анкета** (форма для кандидата); её ответы записываются в данные
кандидата (в candidates).

🔑 ПРОЯСНЕНИЕ ТЕРМИНА «АНКЕТА» (важно — снимает путаницу):
- HR-таб редактора вакансии сейчас называется «Анкета» → переименовать в
  **«Вакансия»** (п.21). Это форма HR-а.
- Кандидатская форма в конце демонстрации = **«системная анкета»** → пишет ответы
  в кандидата. Это РАЗНЫЕ сущности, переименование п.21 как раз разводит их.
- Сюда же относится п.1 («выбор компании в анкете»): тумблер ВКЛ → в ЭТОЙ
  кандидатской анкете показывается выбор компании; и открытый вопрос «кандидат
  выбирает или видит» — это про неё. Связать п.1 + п.35.

---

# ЭКРАН: Таб «Тест» (конструктор теста)
### 36. Здесь создаётся ТЕСТ — тем же конструктором уроков/блоков
Юрий: на табе «Тест» создаётся тест. Используется ТОТ ЖЕ редактор, что и в
«Демонстрации»: слева «Уроки» + «+ Урок», справа редактор урока с блоками контента
(текст / изображение / видео / аудио / файл / callout / чеклист / оценка / запись).
+ те же кнопки шапки (Сохранить, Предпросмотр, «Создать из…»: с нуля / из библиотеки).
ℹ️ Связано с уже существующим скорингом теста (per-option баллы, штраф — см. сессию
03.06, lib/score-test-objective.ts). При разборе свериться: как блоки-вопросы теста
кладутся в скоринг.
❓ Уточнить: «Уроки» — правильное ли слово для теста? (в тесте логичнее «Вопросы»/
«Блоки», а не «Уроки» — возможно переименовать в контексте теста).

---

# ЭКРАН: Вакансия → таб «Настройки» (под-табы вакансии)
Под-табы: Брендинг · Источники · Воронка · Конструктор воронки [Beta] · Сообщения ·
Дожим · AI чат-бот · Расписание · Интеграции.
🔑 СТРУКТУРНО: это per-vacancy ЗЕРКАЛО дефолтов компании (Расписание/Воронка/
Сообщения/Стоп-факторы + Интеграции мы видели на уровне компании). Подтверждает
связку «дефолт компании → переопределение в вакансии». Учесть в IA (большой заход):
дефолты и эти под-табы должны проектироваться вместе, единообразно.

## Под-таб «Брендинг» (Публичная страница вакансии)
Блоки: «Источник брендинга» (тумблер «Использовать брендинг компании» — логотип/
цвета из настроек компании; выкл → свои значения для вакансии) · «Брендинг страницы»
(Название компании, Цвет бренда, Логотип, Слоган/подзаголовок, Домен для публичных
страниц: Бесплатный / Поддомен Company24 [Тариф Бизнес] / Свой домен [Тариф Масштаб],
ссылка + Открыть, Превью шапки с «Откликнуться/Подробнее»).

### 37. «Использовать брендинг компании» — источник и подтягивание
- 37a. Ссылка «настроек компании» сейчас открывает /settings/branding. Должна вести
  в настройки HR (где НЕСКОЛЬКО компаний) и открывать ДЕФОЛТНУЮ/первую компанию.
  Связано с мультикомпанийностью (п.1) и новым IA.
- 37b. При ВКЛ «брендинг компании» подтягивать ВСЁ: логотип, слоган, цвет, название.
  Проверить, что реально подтягивается (не только часть).

### 38. Публичная страница вакансии — кнопка «Откликнуться»
Сейчас «Откликнуться» внизу СЛЕВА. Юрий: должна быть по ЦЕНТРУ или СПРАВА.
(URL примера: company24.pro/vacancy/marketolog-ai-platforma-b2b-udalyonno-...)

---

## СКВОЗНЫЕ ТРЕБОВАНИЯ (продолжение)

### S3. Убрать реальные примеры «orlink.ru» из плейсхолдеров — ВЕЗДЕ
В полях с дефолтными примерами стоит реальный «orlink.ru» (напр. «Ваш сайт: orlink.ru»
в опции Поддомена). Заменить на нейтральный плейсхолдер (напр. «yourdomain.ru» /
«example»). Касается ВСЕХ полей во всём приложении, где показываются такие примеры.

---

# ЭКРАН: Вакансия → Настройки → под-таб «Источники»
Блоки: «Источники кандидатов» (hh.ru подключено: ID вакансии, Откликов 1458,
Необраб., Подключена, Синк, кнопки Открыть на hh / Отвязать; Авито/SuperJob/Яндекс
— «Скоро», Не подключено) · «Источники и ссылки» (короткие ссылки v/… с источником/
названием/кликами/кандидатами, + Создать ссылку) · «Поля формы для анонимных
кандидатов» (системные: Имя, Телефон/Telegram; доп.: Email, Город; + Добавить поле) ·
«Полная страница (HTML)» (Способ 4 — embed-код лендинга для Tilda/Wix/WordPress,
скопировать/предпросмотр/скачать .html).

### 39. Площадки-источники: кнопка «Добавить» вместо россыпи «Скоро»
Юрий: вместо набора плашек «Скоро» (Авито/SuperJob/Яндекс) — кнопка «+ Добавить
источник», открывающая список доступных; подключённые показываются карточками как
hh (и следующий добавляешь так же). Если добавить нечего — писать «Скоро»/скрыть,
чтобы не мозолить глаза. ✅ Рекомендую — согласуется с «прятать Скоро» (п.9).

### 40. Модалка «Создать ссылку»: select «Источник» на всю ширину?
Юрий спрашивает, как правильнее. ✅ Рекомендую да — сделать select на всю ширину,
как поле «Название» под ним (консистентность модалки). Куда ведёт ссылка: Описание
вакансии / Сразу на демо / Сразу на тест (уже есть — коммит 0b1ad8b9).

### 41. ⚠️ БАГИ публичной страницы / предпросмотра
- (a) Новые кастомные поля формы («Добавить поле») НЕ отображаются в предпросмотре
  и, видимо, на публичной странице. Починить: добавленные поля должны рендериться.
- (b) Есть захардкоженный текст, который нельзя отредактировать — вынести в
  редактируемое поле (HR может написать своё).
- (c) Логотип НЕ отображается на публичной странице/в превью (в превью пустой
  квадрат вместо лого). Связано с брендингом/подтягиванием (п.37b).

### 42. CRM-интеграция (под-таб «Интеграции» вакансии) — где её место
Блоки: CRM (Битрикс24/AmoCRM/Другая CRM через API/webhook — все «Скоро») +
«Звонки кандидатам — AI агент» (Скоро, тумблер). Юрий заметил ПЕРЕСЕЧЕНИЕ с
company-level «Интеграции» (п.7,8) и склоняется, что CRM логично ЗДЕСЬ (per-vacancy),
т.к. у каждой вакансии своя воронка, и можно лить по вакансии отдельно или все в одну.
**Согласовано (подтверждает гибрид п.4/8):** ПОДКЛЮЧЕНИЕ CRM (креды) — один раз на
уровне компании; ВЫБОР воронки/роутинг (куда лить кандидатов этой вакансии) — здесь,
per-vacancy. «Пересечение», которое он увидел, — это и есть дубль connection vs routing;
гибрид его убирает. + Звонки AI-агента — тоже per-vacancy сценарий (Скоро).

---

# ЭКРАН: Вакансия → Настройки → под-таб «Расписание»
В целом ок. Блоки: «Рабочие часы и дни» (Соблюдать расписание, Время С..до,
Часовой пояс, Дни недели) + «Нерабочие дни» (Праздники РФ чекбоксами + Свои
нерабочие дни + Добавить).

### 43. UI: время в одну строку + отступ до часового пояса
«С 08:26 до 19:00» сейчас визуально в две строки — сделать в ОДНУ строку. И между
временем и «Часовой пояс» не хватает обычного отступа — выровнять (стандартный
интервал, не больше).

### 44. Переформулировать текст «Свои нерабочие дни»
Сейчас: «Корпоративные отпуска или особые дни — добавьте, если нужно». Юрий: это
НЕ про отпуска сотрудника, а про дни, когда сообщения НЕ должны отправляться
(добавь свой праздник / особый день). Если HR уходит в отпуск — он просто
оставляет вакансию накапливать отклики, это другое. Текст переписать в этом ключе.

---

## СКВОЗНЫЕ ТРЕБОВАНИЯ (продолжение)

### S4. Редактируемые подсказки/микрокопи по всей платформе (admin-managed) ⭐
Большая идея Юрия: служебные тексты-подсказки по всей платформе должны быть:
- КЛИКАБЕЛЬНЫ для копирования (сейчас местами текст не выделяется/не копируется —
  хотелось бы везде);
- РЕДАКТИРУЕМЫ: кликнул → подправил → СОХРАНИЛОСЬ;
- на уровне ПЛАТФОРМЕННОГО АДМИНА правка = ЭТАЛОН для всех клиентов;
- КЛИЕНТ у себя менять НЕ может (только видит/копирует).
По сути — центр управления микрокопи/подсказками (i18n-словарь с переопределением
платформенным админом). Крупная фича, в большой заход; продумать хранилище строк
+ права + UI инлайн-редактирования. Связано с S2 (единый дизайн) и «прятать Скоро».

❓ Пункт «3.» Юрий не дописал — ждём продолжение по «Расписанию».

---

# ЭКРАН: Вакансия → Настройки → под-таб «Воронка» (ЭТО ОДНА ВОРОНКА)
URL: /hr/vacancies/5ae8f734-...?tab=settings&section=funnel
Юрий: «это одна воронка, изучи». Под-таб — длинный, состоит из блоков, которые
ВМЕСТЕ и есть воронка:
1. **Предквалификация** (перед демо): режимы «Сразу демо (реком.)» / «Сначала
   предквалификация → потом демо» / «Только предквалификация (без демо) → стадия
   Анкета». AI задаёт до 3 уточняющих вопросов.
2. **Стоп-факторы по резюме** (тумблеры: Город/релокация, Формат, Возраст, Мин.опыт,
   Документы, Гражданство, Макс. зарплата). При срабатывании → текст отказа, стадия
   rejected.
3. **Стоп-слова → «Отказ»** (чипы: неактуально, не подходит, … — в hh-чате/анкете →
   стадия Отказ, дожим отменяется).
4. **Финальные экраны**: «после видео-визитки (перед анкетой)» (Заголовок/Подзаголовок/
   Кнопка) и «после анкеты» (Заголовок/Текст).
5. **После демонстрации** (вкл/выкл; Режим Автоматический[Скоро]/Ручной; Пороги
   после демо: верхний 75% / нижний 50%; для Отличный/Средний/Не подходит — свои
   тексты + типы встречи + планировщик слотов).
6. **Финальная анкета** = ЭТО И ЕСТЬ «системная анкета» из п.35! Поля Имя/Фамилия/
   Email/Телефон/Telegram/Дата рождения/Город с тумблерами Показывать/Обязательное.
7. **Превью финального экрана**.
8. **Автоответ после заполнения анкеты** (бот шлёт сообщение в hh-чат; задержка
   10с–1ч; «учитывать рабочее время вакансии»; шаблон с {{name}}/{{vacancy}}/
   {{company}}/{{demo_link}}; ссылка на тест).

### 🔑🔑 ЦЕНТРАЛЬНОЕ НАБЛЮДЕНИЕ: воронка ФРАГМЕНТИРОВАНА по многим местам
Одна логическая воронка размазана по: под-таб «Воронка» + «Конструктор воронки
[Beta]» (= Funnel Builder, Group 22) + «Дожим» + «AI чат-бот» + «Расписание» +
стоп-факторы (которые ещё и в дефолтах компании, п.19, и в «AI-профиль кандидата»,
разд. B — ТРОЙНОЕ дублирование стоп-факторов!). Плюс «Финальная анкета» здесь = та
самая системная анкета (п.35). → Большой заход: СВЕСТИ воронку в ОДНО место
(вероятно Funnel Builder как единый конструктор), убрать дубли стоп-факторов/анкеты/
порогов, чтобы был один источник правды. Это, видимо, главный архитектурный узел.
ℹ️ Изучить при аудите: как «Воронка»(legacy-поля) ↔ «Конструктор воронки[Beta]»
связаны (CLAUDE.md: dual-write — funnel config обновляет legacy aiChatbotEnabled/
aiScoringEnabled/aiProcessSettings). Решить судьбу legacy vs Builder.

---

# ЭКРАН: Вакансия → Настройки → «Конструктор воронки [Beta]» (Funnel Builder)
URL: ...?tab=settings&section=funnel-builder
17 блоков (8 из 17 включено), drag-and-drop порядок, у каждого тумблер + шестерёнка
(настройки). Тумблер «Использовать конструктор воронки» + «Применить шаблон» + сброс.
Блоки: AI-скоринг резюме · Стоп-факторы по резюме · Первое сообщение [обяз.] ·
Предквалификация · Демонстрация [обяз.] · Видео-визитка · Анкета [обяз.] ·
AI-скрининг анкеты · Автоответ с тест.заданием · Тестовое задание · Стоп-слова в
чате · Дожим · AI чат-бот · Интервью [обяз.] · Реф-чек · Оффер · Финальный экран
[обяз.]. (= 17 типов из Group 22, CLAUDE.md.)

### 45. ⚠️ БАГ: панель настроек блока (Sheet справа) сломана — очень узкая
При клике на шестерёнку блока должна выезжать боковая панель справа (Sheet/drawer,
как в кандидатах), но сейчас она ОЧЕНЬ УЗКАЯ (сломана) — содержимое обрезано.
Починить ширину Sheet. ℹ️ Реестр настроек блоков: lib/funnel-builder/block-settings.tsx.

### 46. 🔑 РЕШЕНИЕ ПО КОНСОЛИДАЦИИ ВОРОНКИ (главный узел)
Юрий: изучить ОБЕ воронки — legacy под-таб «Воронка» (разд. выше) и этот
«Конструктор воронки [Beta]» — и потом оставить ОДНУ правильную.
Предварительная позиция Claude: **Funnel Builder — естественный единый источник**
(он уже = один упорядоченный список всех 17 ступеней с тумблерами, drag-and-drop и
настройками на блок; ровно «воронка в одном месте»). Блоки legacy-«Воронки»
(предквалификация, стоп-факторы, стоп-слова, финальные экраны, пороги, финальная
анкета, автоответ) маппятся на блоки Builder. Вероятный план: довести Builder до
паритета → перевести всех на него → убрать legacy-таб «Воронка» (и тройной дубль
стоп-факторов). ❗Подтвердить аудитом dual-write (legacy ↔ builder) перед удалением.
ℹ️ Баг п.45 (узкий Sheet) надо чинить, если Builder остаётся (он остаётся) —
значит фикс не зря.

---

# ЭКРАН: Вакансия → Настройки → под-таб «Сообщения»
URL: ...?tab=settings&section=messages
🟢 Юрий: «это сообщения, которые были изначально, и они СЕЙЧАС РАБОТАЮТ». Т.е. это
действующая шаблонная система сообщений (в отличие от «AI чат-бот» — альтернативный
подход). Блоки:
1. **Серия первых сообщений** — до 3 сообщений подряд с задержками (Сообщение 1
   всегда вкл, ОБЯЗАНО содержать {{demo_link}}/{ссылка}; Сообщение 2/3 — опц.).
   Если кандидат ответил/открыл демо — следующие отменяются. Переменные {{name}}/
   {{vacancy}}/{{company}}/{{demo_link}}.
   - **Сообщение для нерабочего времени** — альтернативный текст вне рабочих часов
     (связь с «Расписание»); сообщения 2/3 тогда не шлются.
2. **Первое сообщение** — legacy-заметка: min задержка теперь из «Серии первых
   сообщений», старый параметр НЕ используется (готовый кандидат на чистку).
3. **«Если кандидат хочет созвониться»** — парсер ключевых слов (созвон/позвоните/
   номер/телефон/голос/по телефону) → реакция (предложить слот[Скоро]/сразу слот
   [Скоро]/настоять на демо) + 3 эскалационных шаблона (после №3 не реагирует).
   Дожим на демо.
4. **Частые вопросы кандидатов** — готовые ответы (Зарплата/Формат/График/Локация/
   Оформление/Опыт) с кнопкой «Копировать» для ручной вставки в hh-чат + Удалить +
   Добавить вопрос. Переменные {{name}}/{{vacancy}}/{{salary_from}}/{{salary_to}}/
   {{demo_link}}/{{interview_at}}. ℹ️ Это копируемые тексты — связь с S4.
5. **Аварийное повторное сообщение** — если ссылка битая (по умолч. ВЫКЛ).

### 47. «Сообщения» vs «AI чат-бот» — два подхода к общению (для консолидации)
🔑 Подтверждение к п.46/47: общение с кандидатом реализовано ДВАЖДЫ — рабочая
шаблонная «Серия сообщений + созвониться-парсер + FAQ» (этот таб, РАБОТАЕТ) и
«AI чат-бот» (отдельный таб). В единой воронке (Funnel Builder) это блоки «Первое
сообщение» / «Дожим» / «AI чат-бот» — взаимоисключающие режимы. При консолидации
решить: шаблонный режим и AI-режим как выбор внутри воронки, без дублей текстов.
ℹ️ Legacy-заметка в п.2 (старый параметр задержки не используется) — есть и другой
технический долг в этом блоке; зачистить при разборе.

---

# ЭКРАН: Вакансия → Настройки → под-таб «Дожим»
URL: ...?tab=settings&section=followup. Юрий: «это воронка Дожима сейчас».
Блоки:
1. **Структурированные требования к кандидатам** (для AI-оценки; кнопка «Предложить
   из описания»): v2-скоринг (двухпроходный) активируется при ≥1 must-have, иначе
   упрощённый v1 параллельно. Жёсткие требования (3-5), Желательно (до 5), «Что точно
   НЕ подходит» (deal-breakers, до 3, max −30), Идеальный профиль (1-2 предл.), Веса
   критериев (Σ=100).
2. **AI-фильтр откликов** (тумблер) — AI оценивает резюме перед действием. Пороги:
   верхний (сразу демо) 75 / нижний (отказ) 40. ≥75 демо · 40-74 уточнение · <40
   мягкий отказ. «Что делать с 40-74»: предквалификация / сразу демо / оставить
   «Новый». Текст мягкого отказа ({{name}}/{{vacancy}}). Задержка перед отказом 300
   мин (5ч, в рабочее время; действует на ВСЕ авто-отказы, можно отменить).
3. **Цепочка дожима** (тумблер) — авто-напоминания в hh для не открывших/не дошедших
   демо. Пресеты Выкл/Мягкий/Стандартный(7 касаний/15 дн: Д+1,2,4,6,9,12,15)/Активный.
   Ветка А (не открыл, 9 шаблонов) / Ветка Б (не дошёл, 9 шаблонов). «Использовать
   стоп-слова из Воронки» (P0-22 — стоп-слова уже централизованы в табе «Воронка» ✅).
   «Остановить, если вакансия закрыта».
4. **Дожим по тесту** (тумблер, по умолч. Выкл) — напоминания по тесту: Ветка А (не
   открыл тест) / Ветка Б (открыл, не заполнил), 9 шаблонов. ℹ️ Это моя работа из
   сессии 03.06 (две ветки). Предупреждение в UI: работает независимо от «Напоминаний
   о тесте» в табе «Тест» → дубли; оставить одно (= хвост из HANDOFF 03.06).

### 48. 🔑 СКОРИНГ ДУБЛИРУЕТСЯ ЕЩЁ СИЛЬНЕЕ — критично для аудита (п.26-28)
Критерии оценки резюме теперь найдены в ТРЁХ представлениях:
- «AI-профиль кандидата» (таб Анкета): Идеальный кандидат + Обязательные компетенции
  + стоп-факторы + критерии-веса (Критично/Важно/...).
- «Структурированные требования» (таб Дожим): must-have/nice/deal-breaker/идеальный
  профиль/веса Σ=100 → это вход **v2-скоринга**.
- «Стоп-факторы по резюме» (Воронка + дефолты компании).
Плюс ДВА движка скоринга: **v1 (упрощённый) и v2 (двухпроходный)** работают
параллельно. И ДВА набора порогов: AI-фильтр (75/40, скоринг резюме) vs «После демо»
(75/50, оценка демо). → Аудит обязан разложить: какое поле → какой движок (v1/v2) →
какой порог. Цель: ОДИН источник критериев + понятный один-два движка. Это ядро
«главного узла» (п.46). lib/ai-screen-candidate.ts + v2-скоринг — изучить вместе.

---

# ЭКРАН: Вакансия → Настройки → под-таб «AI чат-бот» [Beta]
URL: ...?tab=settings&section=aichatbot
🔴 Юрий: «чат-бот новый, который ещё ПОКА НЕ РАБОТАЕТ». Т.е. рабочий путь общения =
шаблоны «Сообщения» (п.47); AI чат-бот — будущее/beta, не функционирует.
Полная 4-уровневая архитектура (Group 22, CLAUDE.md):
- AI чат-бот для общения [Beta] (тумблер + «Тестировать» = песочница Group 33;
  «сначала сгенерируйте промпт»).
- «Когда AI отвечает кандидату»: тумблер «Ограничить AI определёнными темами».
- «Промпт для агента»: авто-генерация из профиля компании + анкеты вакансии +
  триггеров (Сгенерировать/Просмотр/Редактировать).
- «Безопасность»: Порог уверенности AI 0.70; Лимит сообщений/день на кандидата 5;
  «Стоп-слова перебивают AI» (стоп-слова из Воронки ✅); Автоматическая защита
  (injection→автоотказ, мат→автоотказ, раздражённый тон→счётчик→отказ на 2-м, эмоц.
  нестабильность→мягкий отказ, вне темы→мягко возвращаем).
- «История срабатываний фильтра оскорблений» (последние 20, undo).
- «Шаблоны автоматических отказов» (injection/мат/повторные/эмоц.нестабильность →
  стадия Отказано).
- «Поведение и тайминги»: задержка перед ответом 10с; «короткое сообщение перед
  ответом» (Response Timing Group 33).
- «Telegram-канал HR для AI-эскалаций».
- «Метрики за 7 дней» (Обработано/Автоответов/Эскалаций/Отказов) + «Квота AI-вызовов
  0/1000» (по компании).
- «AI-наблюдатель» (раз в час второй AI проверяет первого; «Запустить аудит сейчас»).

### 49. AI чат-бот vs «Сообщения» — режимы общения (консолидация п.47)
🔑 Подтверждено: общение реализовано как ДВА режима — шаблоны «Сообщения» (РАБОТАЕТ)
и «AI чат-бот» (Beta, НЕ работает). В единой воронке (Funnel Builder) это блоки
«Первое сообщение»/«Дожим» (шаблонный режим) vs «AI чат-бот» (AI-режим) —
взаимоисключающие. Дизайн: явный выбор режима, общие тексты-шаблоны не дублировать.
ℹ️ Дубли для аудита: «Режим строгости общения» (дефолты компании, п.17) ≈
«Автоматическая защита» здесь (мат/abuse) — вероятно одно и то же, свести.
«Лимит сообщений/день» (5) здесь vs «Безопасность отправки» (мин. задержка) в
«Сообщениях» — разные контроли темпа, не путать.

---

# НАВИГАЦИЯ: модуль «Найм» (sidebar)
Дашборд · Вакансии · Библиотека · Кандидаты · Talent Pool · Аналитика найма ·
Календарь · Настройки найма. (Настройки найма = дефолты компании, которые мы
разбирали выше.)

# ЭКРАН: Календарь (/hr/calendar)
Недельная сетка, фильтры Компания/HR/Мой, Día/Неделя/Месяц, «+ Новое событие».
Модалка «Настройки календаря»: Рабочее время (по дням Пн-Вс с тумблерами/часами/
слотом), Праздничные календари (RU + поиск страны), Отображение (часовой пояс/вид/
номера недель), Видимость календарей (Компания/HR-отдел/Мой + Переговорные), Внешние
календари (iCal: Google Calendar по ссылке + Добавить), Экспорт (ical-URL).

### 50. UI: модалка «Настройки календаря» — поля съехали, не единый стиль
Юрий: в боковой панели/модалке настроек календаря поля немного съехали, стиль не
единый. Привести к общему виду (S2).

### 51. iCal-экспорт-URL — автоподстановка кастомного домена клиента
`company24.pro/api/modules/hr/calendar/ical` — если у компании-клиента свой домен,
здесь тоже должен автоматически подставляться их домен. Связано с кастомным доменом
(п.37/брендинг) и мультикомпанийностью.

### 52. ⚠️ БАГ: кнопка «+ Новое событие» вообще не работает
Создание события из календаря не срабатывает. Починить.

### 53. Дубль: рабочее время + праздники в Календаре vs «Расписание» вакансии
Рабочие часы/дни и праздники РФ настраиваются И в Календаре (модалка), И в табе
«Расписание» вакансии (п.43-44), И в дефолтах компании (Расписание). Проверить при
аудите: один источник или независимые? Свести/связать, чтобы не настраивать трижды.

---

# ЭКРАН: Talent Pool (/hr/talent-pool)
Табы: База · Кампании · Рефералы · Аналитика · Формы.
- **База**: таблица (Имя/Должность/Компания/Источник/Скоринг/Статус[В прогреве/
  Горячий/Идеальный/Холодный]/Контакт). Поиск, фильтры (статусы/источники),
  «Загрузить CSV», «+ Добавить» (модалка: Имя*/Должность/Компания/Откуда узнали/
  Телефон/Email/Telegram/Комментарий), «Запустить кампанию».
- **Кампании**: метрики (Активных/В прогреве/Конверсия) + карточки кампаний
  (Email/Telegram, Отправлено/Открыто/Ответили, конверсия).
- **Рефералы**: реф-программа (ссылки сотрудников company24.pro/ref/…, переходы/
  привёл/нанято/бонус, топ рефереров, правила: 10000₽ за нанятого после исп.срока).
- **Аналитика**: пул-метрики, динамика 6 мес, источники (донат), ср.скоринг по
  источникам, воронка прогрева, AI-инсайт.

### 54. Назначение Talent Pool + ЭТАП 2 (дожим-воронки для пассивных)
Юрий: сюда добавляем кандидатов из откликов / рекомендаций / др. путей, чтобы вести
по ним **дожим-воронки и сценарии прогрева**. Нужны РАЗНЫЕ сценарии: кто откликался
vs кто вообще не откликался. Это **этап 2** (крупное, в большой заход) — переиспользует
дожим/воронку из найма. ℹ️ Связь с консолидацией воронки (п.46): дожим-движок один,
применяется и к вакансиям, и к Talent Pool.

### 55. 🔎 Запрос Юрия: проверить, что в Talent Pool РАБОТАЕТ сейчас, что нет
Сейчас (этап 1, простое) — довести до рабочего хотя бы **добавление** (модалка
«Добавить в Talent Pool», «Загрузить CSV»). Многое похоже на демо-данные (круглые
числа, фейковые имена) — вероятно, UI мокнут. НАДО проверить в коде: что реально
пишется в БД (Добавить/CSV), что мок (Кампании/Рефералы/Аналитика/Формы). Кандидат
в quick-win: рабочее «Добавить», если близко. → Сделаю проверку при паузе (нужен
код-обзор). Юрий пришлёт ещё одну вкладку.

## Talent Pool → таб «Формы»
4 формы (Общая анкета/DevOps набор/Реферал от команды/Анкета с конференции; Активна/
Неактив; Внешн./Внутр.; счётчик заявок). Конструктор формы: Тип (Внешняя/Внутренняя),
Название, Источник, Название размещения, Поля формы (Имя*/Фамилия*/Email*/Телефон/
Должность/Резюме-файл), Слоган, Домен (Бесплатный company24.pro/c/company/… /
Поддомен[Бизнес]/Свой домен[Масштаб]), Превью шапки, HTML-embed (iframe
company24.pro/embed/talent-pool-form). Справа — «Источники и ссылки» (таблица
короткие ссылки /f/… с кликами/кандидатами).

### 56. ⚠️ Talent Pool — ВСЕ данные мок, не использовалось. Проверить добавление
Юрий подтвердил: данные тут мок (не пользовались). Критично проверить, что реально
работает **добавление кандидатов СЮДА из откликов/кандидатов** (а не только мок-UI).
Усиливает п.55 — это первый приоритет по Talent Pool. Формы/источники-ссылки тоже,
вероятно, мок — проверить запись в БД.

### S2-доп. Таблицы — единый стиль (через data-table.tsx), убрать зазоры
Юрий: таблицы (напр. «Источники и ссылки» в Формах) недоделаны — зазор, перенос
заголовка «КОРОТКАЯ ССЫЛКА» в 2 строки, стиль не как везде. Привести ВСЕ таблицы к
единому виду. ℹ️ Память: [[table-unify-style-not-functionality]] — единый ВИД через
data-table.tsx, но действия/функционал у каждой таблицы свои; проверять каждую.
Часть S2 (единый дизайн).

### 57. Переименовать «Talent Pool» → «Резерв»
Везде в UI и навигации «Talent Pool» → **«Резерв»** (русское название, в стиле
остальных разделов). Quick-win-кандидат (но проверить все места употребления:
sidebar, заголовок, табы, ссылки).

---

# ЭКРАН: Кандидаты (модульный, /hr/candidates)
Фильтры: Все вакансии / Все статусы / Все источники / Избранные. Поиск по фамилии.
Сейчас ПУСТО: «0 из 0 кандидатов», «Нет кандидатов — появятся после первого отклика».

### 58. Назначение глобальной страницы «Кандидаты» — подумать + проверить пустоту
Юрий: здесь должны быть, скорее, ВСЕ кандидаты (агрегат по всем вакансиям), но
применение страницы он пока не определил — надо подумать.
⚠️ Странность: показывает 0, хотя на вакансии «Помощник по маркетингу» 1458 откликов.
Проверить: почему пусто — не агрегирует / не реализовано / фильтр по компании (под
аккаунтом «Администратор платформы» нет своих кандидатов)? Решить роль страницы:
глобальный список всех кандидатов vs дубль per-vacancy «Кандидаты». ❓ Под вопросом.

---

# ЭКРАН: Библиотека (/hr/library) — FYI, вроде ок
Юрий: «вроде всё ок, пока не знаю, для информации». Шаблоны анкет/демонстраций/
блоков/тестов для переиспользования в любой вакансии. Табы: Анкеты(0) · Демонстрации(8)
· Тесты(0) · Блоки(7) · Корзина(1). «+ Создать шаблон», поиск.
- Демонстрации: таблица (Название + тег «Системный», Тип [Демо Стандартная/Короткая],
  Должность, Блоков, Создан, …). Есть «AI: Новая вакансия», «Маркетолог Company24.Pro
  v8» (+копии), системные демо по ролям.
- Блоки(7): «Блок компании — Приветствие/О продукте/Рынок и клиенты/О руководителе/
  Стек/Команда/Что дальше» (все Системный) — переиспользуемые блоки для демо.
- Корзина: удалённые шаблоны с «До удаления» (25 дн) — мягкое удаление, как у вакансий.
ℹ️ Связь: это источник для «Создать из… → Из библиотеки» (демо/тест/анкета) и
платформенные/компанийные шаблоны воронки (CLAUDE.md). Замечаний нет — на будущее.
ℹ️ Таблицы здесь — тоже под единый стиль (S2-доп).

---

# ЭКРАН: Дашборд (/hr/dashboard, глобальный) + Аналитика вакансии
**Дашборд (по всем вакансиям)** под аккаунтом «Администратор платформы»: всё 0
(Активных вакансий/Откликов/Прошли демо/Нанято/Конверсия), AI-ассистент инсайты 0,
Прогресс/Воронка/Активные вакансии пусто. Виджеты: 5 KPI-плиток, AI-ассистент
(Самая горячая/Висят без ответа/Топ-кандидаты ждут/Открыли демо), Прогресс кандидатов,
Воронка найма, Активные вакансии.

**Аналитика вакансии** (tab=analytics) под ролью «Директор»: РЕАЛЬНЫЕ данные —
Воронка найма (1505 канд., демо 1502, интервью 88), Всего 1505, Конверсия 0.0%,
Ср.AI-скор 44, Дней активна 27; Конверсия между этапами (Новый→Демо 100%, Демо→
Решение 6%, Решение→Интервью 0%); Источники (hh 1499/other 5/preview 1); Распределение
AI-скора (0-40:68, 41-70:60, 71-100:24). ✅ Аналитика вакансии работает.

### 59. Глобальный Дашборд/Кандидаты пусты под платформенным админом
Подтверждает п.58: глобальные страницы (Дашборд, Кандидаты) скоупятся по компании, и
у аккаунта «Администратор платформы» своих вакансий/кандидатов нет → 0. А под ролью
«Директор» (компания) данные есть (1505). → Проверить: для ОБЫЧНОГО HR глобальный
Дашборд должен агрегировать его вакансии (а не 0). Скорее не баг, а контекст роли —
подтвердить. Замечаний по виджетам нет.

---

# ДОБАВЛЕНО ПОСЛЕ БРАУЗЕРНОЙ ПРОВЕРКИ (логин под Директором)

### 60. Календарь: учёт пересечений между календарями (cross-calendar conflict)
Юрий: (a) нужен ли единый вид «Все» (Компания+HR+Мой в одной сетке) — под вопросом;
(b) ГЛАВНОЕ: если в «Мой» отметил дело, а в «Компания» есть планёрка — система
должна это учитывать: либо не дать назначить на это время, либо минимум показать
«в это время общая планёрка», а HR сам решит.
⚠️ Текущее состояние (по коду event-modal.tsx:101-120): детекция конфликтов ТОЛЬКО
по ПЕРЕГОВОРНЫМ («Переговорная занята: …»). Пересечение по ВРЕМЕНИ между календарями
(моё ↔ компания/HR) НЕ проверяется. → Нужно: проверка занятости по времени против
видимых календарей при создании события + предупреждение/блок.
ℹ️ В настройках календаря уже есть «Видимость календарей» с цветами (Компания/HR/Мой)
— основа для вида «Все» (наложение слоёв). Рекомендация: вместо вкладок-фильтров
сделать переключатели слоёв (как в настройках), тогда «Все» = включить все слои.

### 61. Конструктор воронки [Beta]: НЕ хватает блока «Тест» (квиз-конструктор)
Юрий: в Builder есть блок «Тестовое задание» (задание→ответ→AI-проверка), но это НЕ
то. Не хватает блока **«Тест»** — той вкладки «Тест», где создаётся сам тест (квиз с
вопросами и per-option скорингом, lib/score-test-objective.ts). Добавить отдельный
блок «Тест» в Конструктор воронки. ℹ️ Различать: «Тестовое задание» (домашка с
AI-проверкой) ≠ «Тест» (структурированный тест/квиз со скорингом). Связано с
консолидацией воронки (п.46) и существующим скорингом теста (сессия 03.06).

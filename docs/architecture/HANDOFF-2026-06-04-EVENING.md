# HANDOFF — вечер 04.06.2026 (для нового чата, автономный режим)

> Новый чат: прочитай ЭТОТ файл первым. Юрий уехал на 6-7 часов и хочет, чтобы ты
> РАБОТАЛ АВТОНОМНО — брал задачи из очереди по одной, реализовывал, коммитил,
> выкатывал на СТЕЙДЖИНГ, не спрашивал, не останавливался. Делать-делать-делать.

## 🔴 РЕЖИМ РАБОТЫ (критично)
- **Автономно**: бери задачи из «ОЧЕРЕДИ» ниже по порядку, делай, коммить в develop,
  выкатывай на стейджинг, отмечай в доках. Не спрашивай Юрия по мелочам.
- **НЕ деплоить ПРОД сам** (company24.pro, 1505 живых кандидатов). Всё копи на
  стейджинге. Прод-деплой — пачкой, когда Юрий вернётся «на пару минут» (10 мин) или
  напишет «прод». Если сломать прод-билд в его отсутствие — кандидаты пострадают.
- **НЕ включать outward** без явного OK Юрия: AI-чат-бот рассылка, авто-отказ (D5),
  прогрев Резерва, Битрикс-отправка, Phase 3 флаг на полигоне. Они и так выключены
  по умолчанию — НЕ трогай переключатели.
- **Аккуратно, без отсебятины и спешки.** Юрий сегодня расстроился, что я наклёпывал
  быстро и сделал дубли/лишнее. Делай ОДНУ вещь качественно, проверяй сборкой, не
  угадывай — если неясно, бери следующую задачу из очереди, где всё понятно.

## ОЧЕРЕДЬ ЗАДАЧ (🟢 безопасно, без outward) — делай по порядку
1. **B5/#31** — ложное «Критично: О компании отсутствует» (в процессе). Источник:
   серверный анализ `app/api/ai/vacancy-advisor/route.ts` — он считает «О компании»
   незаполненным, хотя у вакансии текст в `data.companyDescription`. Найти проверку и
   учесть поле вакансии (карточка CompanyDescriptionCard уже учитывает оба — проблема
   в серверном `errors`). + #29: описание показано и в форме (блок 4), и в AI-панели —
   решить дубль.
2. **Vacancy-side мультикомпании** (O1 ч.2) — при создании вакансии выбор компании из
   списка `companies.hiringDefaultsJson.brandCompanies` (+ основная). Показать селектор
   в редакторе вакансии когда `showCompanySelector`. ⚠️ Правило Юрия (железобетон):
   ПЕРВАЯ компания всегда основная из кабинета (Брендинг), не редактируется; добавленные
   = №2+. Не дублировать описание.
3. **#50** — Календарь: стиль модалки «Настройки календаря» (поля съехали, единый вид).
4. **#39** — Источники кандидатов: «+ Добавить источник» вместо россыпи «Скоро»
   (Авито/SuperJob/Яндекс), как карточка hh.
5. **S2-доп** — унификация таблиц (единый вид через data-table.tsx; функционал не трогать,
   только стиль; проверять каждую — память table-unify-style-not-functionality).
6. **#34** — проверить «Заполнить из…» (Вставить текст / Из библиотеки / Загрузить файл) —
   работают ли; если нет, доработать/скрыть.
7. **C6** — напоминания об интервью (24ч/2ч): реально ли cron шлёт; проверить/починить.
Дальше — из `ROADMAP-ETA.md` (🟢-задачи). НЕ бери 🔴(outward)/⚠️(риск/прод) без Юрия.

## 📋 ПРОГРЕСС АВТОНОМНОЙ СЕССИИ (04.06 вечер, на стейджинге)
Коммиты в develop, собрано локально+на стейджинге (EXIT 0), pm2 reload, страницы 302 (ок).
1. ✅ **B5/#31 + #29** (коммит b18a6c83). Ложное «Критично: О компании отсутствует» —
   добавлена детерминированная пост-обработка секции `company` в AI-пути
   `app/api/ai/vacancy-advisor/route.ts` (если описание есть — ok; пусто — максимум
   warning, никогда не error). #29: карточка «О компании» в AI-панели больше не
   дублирует текст (он в блоке 4 формы рядом) — компактное подтверждение; убран
   мёртвый чекбокс «Включить в описание».
2. ✅ **Vacancy-side мультикомпании (O1 ч.2)** (коммит 494ba9ed). Анкета, блок
   «О компании»: селектор «Компания вакансии» когда в Настройках найма включён
   showCompanySelector и есть бренды. №1 «Основная: {из кабинета}» (не редактируется),
   №2+ — бренды из hiringDefaultsJson.brandCompanies. При выборе подставляется
   описание выбранной компании. brandCompanyId хранится в descriptionJson.anketa
   (без миграции). Публичная страница вакансии: API резолвит brandCompanyId →
   кандидат видит название бренда вместо основной компании. Описание кандидат и так
   берёт из блока 4 (под бренд) — дубля нет. ⚠️ ТРЕБУЕТ ТВОЕЙ ПРОВЕРКИ железобетон-
   правила в браузере (визуально отсюда не могу — DNS/сеть с Mac закрыты).
3. ✅ **#50** (коммит 29bbb2b8). Модалка «Настройки календаря»: `SheetContent` без
   горизонтального padding (а `SheetHeader` с p-4) → поля прижаты к краям. Добавлен
   `px-4 pb-6` телу Sheet в `app/(modules)/hr/calendar/page.tsx` — выровнено под шапку.
4. ✅ **#39** (коммит 29bbb2b8). `components/hr/integrations-content.tsx`: вместо россыпи
   faded-карточек «Скоро» (Авито/SuperJob/Яндекс) — одна карточка «+ Добавить источник»
   в стиле hh, по клику раскрывает список площадок со «Скоро». Бэкенд не трогал.
5. ✅ **S2-доп унификация таблиц — HR-модуль** (коммиты da8cbd09, c4a69fb9). 9 сырых
   `<table>` → примитивы data-table.tsx (TableCard/DataTable/DataHead/DataHeadCell/
   DataRow/DataCell), единый вид шапки/ячеек/ховера, функционал 1:1:
   positions, departments, pulse-surveys, flight-risk, talent-pool (с кастомной
   сортировкой), adaptation/assignments, adaptation/analytics, vacancy-аналитика
   (источники), hiring-settings (шаблоны). НЕ трогал матрицы со sticky-колонками
   (vacancies/[id]/compare, test-table) — там спец-раскладка, канон-список сломал бы.
   Прочие модули (sales/warehouse/dialer/b2b/knowledge/learning/marketing) — не делал
   (вне HR-фокуса; если нужно — скажи). ⚠️ Визуально отсюда проверить не могу
   (DNS/сеть с Mac закрыты) — глянь HR-таблицы на стейджинге.
6. ✅ **#34** — ПРОВЕРЕНО, всё работает. Дропдаун «Заполнить из…» в
   `app/(modules)/hr/vacancies/[id]/page.tsx:2367` — все 4 пункта подключены к реальным
   эндпоинтам (нет mock): Вставить текст→`/api/ai/parse-vacancy`; Из библиотеки→
   `loadLibrary`+`handleApplyTemplate`; Загрузить файл→`/api/modules/hr/vacancies/parse-file`;
   Импорт hh.ru→`/api/vacancies/[id]/hh-import`. Эндпоинты на месте. Чинить/скрывать нечего.
7. ✅ **C6 напоминания об интервью (24ч/2ч)** (коммит 06ac9b96, на стейджинге, E2E-проверено).
   Юрий уточнил: источник — ЗАПИСИ КАЛЕНДАРЯ (calendar_events, type='interview'),
   а не mock-модуль «Интервью». Реализовано:
   - Cron `/api/cron/interview-reminders` (GET/POST, X-Cron-Secret): находит интервью
     в окне 24ч и 2ч до start_at, шлёт ВНУТРЕННЕЕ напоминание HR/организатору —
     in-app (notifications, userId=createdBy) + Telegram-канал компании (sendToCompanyChannel,
     тихо пропускается если не подключён). НЕ сообщение кандидату (не outward).
   - Миграция 0172: calendar_events.remind_24h_sent_at / remind_2h_sent_at (идемпотентно,
     без повторов; проверено — повторный вызов 0 дублей) + частичный индекс.
     ПРИМЕНЕНА на стейджинге; на прод — при деплое.
   - Уважает тумблеры hiringDefaults.schedule.remind24h/remind2h (дефолт ВКЛ).
   - E2E на стейджинге: 2 тест-события (1.5ч/20ч) → sent2h:1, sent24h:1, тексты+время
     корректны, метки проставлены; повтор — skipped, дублей нет. Логи в cron_runs.
   ⚠️ НЕ в crontab — активирует Юрий. ПРОД-строка (раз в час) в шапке роута. На СТЕЙДЖИНГ
   crontab НЕ ставить: telegramBotToken/chatId скопированы с прода → крон слал бы реальным
   компаниям. На проде — это их собственный канал (внутреннее, как и задумано).
   Возможное расширение (по твоему OK): напоминание самому КАНДИДАТУ (адрес/ссылка) —
   это уже outward + нужна привязка кандидата к событию календаря (сейчас её нет).

### ИТОГ автономной сессии (вечер 04.06):
СДЕЛАНО+ЗАДЕПЛОЕНО на стейджинг: задачи 1, 2, 3, 4, 5 (9 HR-таблиц) — коммиты
b18a6c83, 29bbb2b8, 494ba9ed, da8cbd09, c4a69fb9. ВЕРИФИЦИРОВАНО рабочим: задача 6.
ОТЛОЖЕНО (нужно твоё продуктовое решение): задача 7 (модуль интервью — макет).
Прод НЕ трогал. Реальный outward (отправка кандидатам) НЕ включал.
⚠️ Визуальную проверку фронта отсюда сделать НЕ МОГ (DNS/сеть с Mac в этом
окружении закрыты, браузер не резолвит new.company24.pro) — проверял сборкой +
серверным HTTP (302/онлайн). Глянь сам на стейджинге, особенно O1-селектор и HR-таблицы.

## ✅ СДЕЛАНО СЕГОДНЯ (на стейджинге, ждёт прод-деплоя вечером)
Огромный день. Всё в develop (до коммита ~a7a573f6), на new.company24.pro:
- **Резерв ПОЛНОСТЬЮ** (5 табов real, без mock): База (add/CSV), Кампании, Рефералы,
  Аналитика, Формы + публичная форма /f/{slug} → заявки в «Базу» (E2E проверено).
  Таблицы: referral_links, talent_campaigns, talent_pool_entries, talent_forms,
  form_tracking_links (миграции 0167-0171).
- **Поддомен {sub}.company24.pro** — ИНФРА НА ПРОДЕ (DNS wildcard + TLS-серт
  *.company24.pro до 02.09 + nginx wildcard, проверено 200). Код (middleware → careers
  страница) в develop. После прод-деплоя: HR задаёт поддомен в Брендинге → открывается.
- **Phase 3** (рантайм на Funnel Builder за флагом funnel_runtime_enabled, миграция 0166,
  8 тестов) — ждёт reconcile+включения (PHASE3-RUNBOOK.md), НЕ включать без Юрия.
- **D5** тумблер авто-отказа (default off). **D6** веса — уже работали (проверено).
- **hiring-settings**: O2 (опросы 30/60/90 реальные), O1 (список компаний-брендов;
  первая = из кабинета), O4 (Битрикс «Проверить связь» + реальная отправка лида).
- Мелочи: B9 (вёрстка hh), #38 (кнопка Откликнуться по центру), B6 (лого в embed
  абсолютный), #11 (Я.Телемост), поддомен в Брендинге, вариант логотипа сайдбара
  (с подложкой/без), смена пароля (была — показал где).

## ДОСТУПЫ / ИНФРА
- Прод: `ssh tz` (root@5.42.125.91), /var/www/my-komanda, pm2 my-komanda:3000.
- Стейджинг: /var/www/my-komanda-new-staging, pm2 my-komanda-new-staging:3001,
  БД mykomanda_new_staging (копия прода). Домен new.company24.pro за basic-auth
  **yuri / Komanda2026**. Залогинен как платформенный админ (тест-аккаунт j.stumpf@yandex.ru).
- AI-прокси: рижский VPS 155.212.231.73:8080 (память claude-proxy-riga-vps).
- Wildcard-серт продлить до 02.09 (Timeweb DNS-01; можно автоматизировать через
  Timeweb API — у Юрия есть «API и Terraform», нужен токен).

## РАБОЧИЙ ЦИКЛ ДЕПЛОЯ НА СТЕЙДЖИНГ (твой основной)
```
cd ~/Projects/my-komanda && git add <files> && git commit && git push origin develop
ssh tz 'cd /var/www/my-komanda-new-staging && git pull origin develop \
  && [sudo -u postgres psql -d mykomanda_new_staging -f drizzle/NNNN.sql если миграция] \
  && pnpm build && pm2 reload my-komanda-new-staging'
```
Билд локально перед коммитом: `pnpm build` (фоном, ждать exit 0). 65 baseline tsc-ошибок —
не твои, билд их игнорит (next.config ignoreBuildErrors).

## ПРОД-ДЕПЛОЙ (только когда Юрий вернётся/скажет «прод»)
1. Дамп: `ssh tz "pg_dump 'postgresql://mykomanda:Comp2024!@localhost:5432/mykomanda' -Fc -f ~/mykomanda-$(date +%F).dump"`
2. `ssh tz 'cd /var/www/my-komanda && git pull origin develop && [миграции 0166-0171] && pnpm build && pm2 reload my-komanda --update-env'`
3. Phase 3: НЕ включать флаг без явного OK (PHASE3-RUNBOOK.md: reconcile ai_chatbot блок → флаг).

## КЛЮЧЕВЫЕ ДОКИ
- ROADMAP-ETA.md (очередь+ETA), STATUS-INTAKE.md (61 пункт intake со статусом),
  MASTER-BACKLOG.md, PHASE3-RUNBOOK.md, WILDCARD-SUBDOMAIN-RUNBOOK.md.
- Память: working-mode-autonomous-terminal, claude-proxy-riga-vps,
  funnel-consolidation-scope-one-vacancy, deploy-downtime-offpeak.

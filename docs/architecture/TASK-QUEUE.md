# Очередь задач (собираем сейчас → запускаем по команде)

> Правило: задачи сюда ДОБАВЛЯЮТСЯ по ходу. НЕ выполнять, пока Юрий не скажет
> «запускай» (можно по одной: «запускай #1» или пачкой). Всё правится на develop →
> стейджинг (new), прод — потом.
> Статусы: ⏳ в очереди · ▶️ в работе · ✅ готово (на стейджинге) · 🚀 на проде

---

## ✅ #1 — Починить «Роли и доступ» (`/settings/roles`)
- **Симптом:** изменения не сохраняются (0 обращений к API — локальный стейт).
- **Файл:** `app/(platform)/settings/roles/page.tsx` (~177 строк).
- **План:** найти/задействовать хранилище ролей-доступа (есть `/api/modules/hr/module-roles`
  с PUT — проверить, подходит ли; иначе добавить эндпоинт), подключить load+save с
  оптимистичным апдейтом и откатом при ошибке (как сделали в «Команда»).
- **Проверка:** изменить доступ → сохранить → перезагрузить → осталось.

## ✅ #2 — Починить «Расписание» (`/settings/schedule`) — STANDALONE
- **Симптом:** общее рабочее время компании не сохраняется (грид Пн–Вс — локальный
  стейт; блок индивидуальных графиков на mock + PATCH в несуществующий `/api/team/[id]`).
- **Файл:** `app/(platform)/settings/schedule/page.tsx` (~632 строки).
- **ВАЖНО (решение Юрия):** оставить **standalone** — НЕ привязывать к календарю,
  вакансиям, hiring-settings или крону. Просто сохранять своё значение.
- **План:** добавить хранилище под рабочее время компании (вероятно новая jsonb-колонка
  на `companies`, напр. `company_work_schedule` — миграция; на проде применять в пачке) +
  GET/PUT, подключить load+save грида. Решить судьбу блока «индивидуальные графики»
  (mock + битый эндпоинт) — починить или скрыть.
- **Память:** см. `schedule-three-systems-keep-separate` (три расписания НЕ объединять).

## ✅ #3 — «Юр.документы» (`/settings/legal`): рабочий генератор политики + контактный блок
- **Диагноз (проверено по БД):** генератор «Сгенерировать шаблон» (`POST /api/companies/privacy-policy`)
  требует И ИНН, И `companies.email`. У Орлинка **ИНН есть** (7715431248), а **email/phone/subdomain
  ПУСТЫЕ** → ошибка «Заполните ИНН и email» (сбивает с толку — не хватает именно email).
  ИНН берётся нормально. Реквизиты в `companies` (name, inn, fullName, legalAddress, email, phone, subdomain).
- **Сделать:**
  1. **Контактный email компании** — добавить поле в Настройки → Компания (обязательное для
     юр-страниц), хранить в `companies.email`. Поле **свободное** — клиент вписывает ЛЮБОЙ свой
     email. По умолчанию показывать подсказку `legal@<домен>` (просто placeholder, не навязывать).
     Рядом — **небольшое не-угрожающее предупреждение** (info, не error): «Укажите реальный ящик —
     его нужно завести и регулярно проверять, на него будут приходить обращения по политике
     конфиденциальности / запросы по ПДн». Тон спокойный, информирующий.
  2. **Контактный блок** (название + email + телефон + юр.адрес «куда обращаться») — вынести
     наверх страницы `/settings/legal` И в публичную политику конфиденциальности; тянуть из companies.
  3. **Сделать вкладку «Юр.документы» видимой директору** (сейчас `roles:['platform_admin']` в
     settings-header + только platform_admin в getVisibleSettings) — добавить `legal` директору.
  4. Генератор уже читает inn+email — заработает, как только email задан. Текст шаблона —
     `lib/legal/default-privacy-policy.ts` (подставляет name/inn/legalAddress/email).
  5. **Дата изменений** — показывать на ПУБЛИЧНОЙ странице политики («Действует с / Обновлено: <дата>»)
     и в редакторе бейдж «Сохранено: <дата, время>». Ставить **АВТОМАТИЧЕСКИ при сохранении**
     (`privacyPolicyUpdatedAt` уже есть в схеме и пишется на PUT). Рекомендация (юридически
     надёжнее): дату вручную НЕ редактировать — авто-дата = когда документ реально изменён.
     (Юрий допускал и ручную правку «если юридически ок» — я советую авто; финально подтвердить
     при запуске.)
- **Файлы:** `app/(platform)/settings/legal/page.tsx`, `components/legal/*`,
  `app/api/companies/privacy-policy/route.ts`, `lib/legal/default-privacy-policy.ts`,
  `app/(platform)/settings/company/page.tsx` (поле email), `lib/auth.tsx` + `settings-header.tsx` (видимость).
- **✅ Решено (email):** `legal@…` — только подсказка-placeholder, НЕ навязываем. Поле свободное,
  клиент вводит любой свой email. Под полем — спокойное info-предупреждение, что ящик нужно
  завести и проверять (входящие обращения). Без угроз.
- **Проверка:** задать email в настройках → на /settings/legal «Сгенерировать» подставит реквизиты →
  сохранить → публичная страница показывает контакты.

## ✅ #4 — Доступ к настройкам компании: редактирование только директору (сквозное правило)
- **Правило (Юрий):** во ВСЁМ разделе «Настройки компании» все **общие/компанийские** настройки
  (не пользовательские) может **редактировать только Директор**. Остальные роли:
  - по возможности **НЕ видят** их вообще (предпочтительно — скрыть таб/блок);
  - где увидеть всё же нужно — показывать **read-only** (видно, но редактировать нельзя: поля
    disabled, кнопки «Сохранить» скрыты/неактивны).
- **Личные (пользовательские) настройки — у каждого свои, редактирует сам:** Профиль, и свои
  Уведомления (notification_preferences — per-user). Их НЕ блокировать.
- **Компанийские (director-only edit):** Компания, Брендинг, Расписание(компании), Интеграции,
  Команда, Роли и доступ, Тариф и оплата, Юр.документы.
- **Механизм:** гейт по роли `director` (или права `manage_company`/`manage_team`/`manage_billing`,
  если решим тоньше). Видимость табов — уже частично в `getVisibleSettings` (lib/auth.tsx). Для
  read-only — общий хук/контекст `canEditCompanySettings` + прокинуть в страницы (disabled инпутов
  и скрытие/disable кнопок сохранения). Дополнительно — серверная защита: PATCH/PUT компанийских
  эндпоинтов должны проверять роль (иначе read-only обходится через API).
- **Файлы:** `lib/auth.tsx` (getVisibleSettings + helper роли), все `app/(platform)/settings/*/page.tsx`,
  соответствующие `app/api/companies/*`, `app/api/team/*`, `app/api/branding/*` и т.п. (серверные проверки).
- **Проверка:** зайти под не-директором (hr_lead / hr_manager) → компанийские настройки либо не
  видны, либо read-only; попытка сохранить через API — отклоняется (403).

## ✅ #5 — Приглашения в команду: убрать путаницу двух механизмов (UX, НЕ слияние)
- **Контекст (проверено):** на `/settings/team` ДВА механизма, они НЕ дубль, а разные:
  - «Ссылка для присоединения» `/join/{код}` — `companies.joinCode/joinEnabled`, одна постоянная,
    роль ВСЕГДА «Сотрудник», без срока/лимита, читаемый код, вкл/выкл.
  - «Ссылки-приглашения» `/invite/{token}` — таблица `inviteLinks`, много, ЛЮБАЯ роль, срок+лимит,
    случайный токен (`randomBytes`).
- **Безопасность токена:** invite выдаёт роль (вплоть до HR/Директора), поэтому случайная часть
  ОБЯЗАТЕЛЬНА (читаемый/угадываемый адрес = дыра). join читаемый безопасен (роль = Сотрудник + выключатель).
- **Сделать (UX-ясность, без слияния):**
  1. Оформить как ОДИН раздел «Приглашения» с двумя явно подписанными частями:
     «Публичная ссылка (роль: Сотрудник)» и «Персональные приглашения (роль + срок)».
  2. Добавить короткие пояснения «когда что использовать».
  3. У invite выводить «Описание» (`label`) как понятную метку рядом со ссылкой.
  4. **Короче + бренд (запрос Юрия):** формат `/invite/{companySlug}-{shortRandom}` — добавить
     название компании в адрес и сократить случайную часть с 24 до ~10 символов base62
     (62^10 ≈ 8·10^17 — против перебора достаточно, плюс одноразовость + срок). НЕ убирать
     случайную часть целиком. Менять генерацию в `POST /api/invites` (`randomBytes(18)` → короче +
     префикс slug) и парсинг в `app/(auth)/invite/[token]/page.tsx` + `/api/invites/accept`
     (учесть, что в токене теперь есть `slug-`). companySlug брать из `companies.joinCode`/subdomain/
     транслита названия.
  5. **Модалка не влезает (запрос Юрия):** сейчас контент обрезается по правому краю (поле
     «Описание» и иконки копировать/открыть уходят за границу), приходится скроллить внутри.
     Починить: расширить модалку (увеличить max-width) И/ИЛИ сделать контент компактнее/адаптивным
     (грид полей в одну колонку на узком, ссылки переносятся, иконки не вылезают). Файл модалки —
     в `app/(platform)/settings/team/page.tsx` (Dialog «Ссылки-приглашения»).
- **Файлы:** `app/(platform)/settings/team/page.tsx` (обе секции + модалка), `app/api/invites/route.ts`
  (генерация токена), `app/(auth)/invite/[token]/page.tsx` + `app/api/invites/accept/route.ts` (парсинг).
- **Проверка:** на /settings/team два чётко разделённых блока; invite-ссылка вида
  `…/invite/orlink-Ab3xK9pQ` (короткая, с брендом); модалка влезает без внутреннего скролла.

## ⏳ #6 — Единая «Настройка воронки» (объединить разбросанные блоки, синхрон с hh.ru)
- **Проблема (Юрий):** настройки воронки разбросаны по hiring-settings и пересекаются:
  1. «Воронка → действия в hh.ru» (`hiringDefaults.stageHhActions`) — что слать в hh на стадии (делали сегодня).
  2. «Дефолтный сценарий воронки» (`hiringDefaults.funnelScenario` — пресет) — вкладка «Воронка».
  3. «Автоматизация» (`hiringDefaults.automation`: autoDemo/autoInvite/minScore/autoReject) — там же.
  4. Per-vacancy редактор воронки (`components/vacancies/funnel-tab.tsx`) — переопределяет на вакансии.
  5. «Автоматический сбор обратной связи» (`hiringDefaults.feedbackSurveys` — опросы 30/60/90 дней
     ПОСЛЕ найма) — Юрий: отнести сюда же, это хвост воронки/процесса (после найма → адаптация).
  Блоки про одно (воронку/процесс) в разных местах → дубль/путаница.
- **Сделать (отдельный этап):** свести в ОДИН связный раздел «Настройка воронки»:
  стадии (пресет + вкл/переименование/цвет) + действие в hh.ru на каждую стадию +
  автоматизация — вместе, один источник. Синхронизировано с hh.ru. Company-дефолт +
  per-vacancy override (FunnelTab) на той же модели.
- **«Убрать из табов»:** вынести воронку из мешанины вкладок в отдельный понятный экран/шаг
  (Юрий: «давай сделаем это отдельный этап настройка воронки»).
- **Файлы:** `app/(modules)/hr/hiring-settings/page.tsx` (блоки stageHhActions/scenario/automation),
  `components/vacancies/funnel-tab.tsx`, `lib/stages.ts`, `lib/hh/sync-stage.ts`, `lib/funnel-builder/*`.
- **Связанное (отдельно решить):** Битрикс/CRM сейчас company-wide, кандидаты идут лидами
  (crm.lead.add), per-vacancy маршрутизации «вакансия→своя воронка Битрикса» НЕТ — если нужна,
  это отдельная доработка (добавить выбор воронки/категории Битрикса на вакансию).
- **ВАЖНО:** это большой редизайн (outward-механика hh) — НЕ включать реальные действия hh без
  OK; сначала структура/UI, проверка на стейджинге.

## ⏳ #7 — Редизайн всей страницы «Настройки найма» (`/hr/hiring-settings`) + правки контента
> Связано с #6 (единая воронка) — делать заодно. Файл: `app/(modules)/hr/hiring-settings/page.tsx`.
- **Конкретные правки контента:**
  1. **Интервью → длительность НА КАЖДЫЙ способ.** Сейчас способы (Zoom/Я.Телемост/Google Meet/
     телефон/офис) + отдельный блок «Слоты для интервью» (одна длительность 45м + буфер 15м).
     Юрий: длительность у каждого способа своя → добавить селектор длительности СПРАВА у каждого
     способа, убрать общий блок «Слоты». Буфер можно оставить общим (решить).
  2. **«Звонок по телефону» → на первое место** в списке способов.
  3. **Из вкладки «Сообщения» убрать «Режим строгости общения»** (abuse-mode). По смыслу это
     поведение AI-чат-бота → его место в настройках AI чат-бота (вакансия/AI-раздел), не тут.
  4. **Стоп-факторы** (Юрий склоняется убрать отсюда): они реально настраиваются на вакансии.
     Моя рекомендация — НЕ удалять совсем (company-дефолты = стартовые значения новой вакансии,
     полезно), но сделать второстепенными/свёрнутыми, либо перенести в раздел «воронка/дефолты
     вакансии». Финально решить при запуске.
- **Дизайн/раскладка (вопрос Юрия «табы или нет»):** моя рекомендация — уйти от текущих 4
  размытых табов (Расписание/Воронка/Сообщения/Стоп-факторы), они дробят связанное и плодят
  путаницу (см. #6). Лучше — ОДНА прокручиваемая страница с понятными секциями по смыслу +
  липкая боковая мини-навигация по секциям:
  1. **Воронка и автоматизация** (из #6 — стадии + действия hh + автоматизация + опросы 30/60/90).
  2. **Интервью** — способы (с длительностью на каждый) + часы записи + напоминания.
  3. **AI-общение** — темп отправки (send-delay) [+ режим строгости, если решим оставить в company].
  4. **Стоп-факторы (дефолты)** — свёрнуто, второстепенно.
  5. **Служебное** — корзина, хранение данных (ФЗ-152), webhooks/Битрикс, мультикомпания.
  (Альтернатива, если не хочется длинной страницы: 2–3 крупных осмысленных таба вместо 4 мелких.)
- **Проверка:** страница читается сверху вниз без «где же это», связанное рядом, у способов
  интервью своя длительность, телефон первый, лишних блоков нет.

---

## Кандидаты на проверку (ещё не задачи — надо подтвердить, есть ли баг сохранения)
- `/settings/company`, `/settings/profile`, `/settings/billing`, `/settings/integrations` —
  запись на сервер есть, но возможна «не та кнопка» (как было в Брендинге). Проверить
  реальное сохранение каждой.

## Уже сделано в эту сессию (на стейджинге, ждут прод-наката)
- ✅ Брендинг: выбор «с подложкой/без» сохраняется + компактное превью без рамки.
- ✅ Команда: смена роли/прав/статуса участника сохраняется (PATCH/DELETE в `/api/team`).
- ✅ Уведомления: мастер-тумблер «Все уведомления» персистится (per-event и так сохранялись).
- ✅ Меню настроек: сайдбар-флайаут = вкладкам (состав+порядок, +Брендинг); под аватаром
  «Компания»+«Профиль» → «Настройки компании».
- ✅ Вкладка «Расписание» перенесена из «Дефолты вакансии» в «Профиль и компания».
- ✅ Bundle-guard (авто-перезагрузка при ChunkLoadError) — уже на проде.

# OPEN-QUESTIONS — что ещё нужно от Юрия

> Вопросы, которые блокируют или влияют на работу. Отвеченные — в DECISIONS.md.

## Блокирующие / важные
- **Q1. Deep-link к табам активной вакансии.** Сейчас приход на `?tab=settings/anketa/
  analytics` для АКТИВНОЙ вакансии намеренно кидает на «Кандидаты» и чистит `?tab`
  (хотфикс P0-50, page.tsx:590-597). Это блокирует: (а) браузер-проверку пустого Sheet,
  (б) шаринг/закладки на настройки активной вакансии. **Разрешить уважать явный `?tab=`?**
  (рекомендация Claude: да — респектить явный URL-таб, дефолт «Кандидаты» оставить только
  когда `?tab` отсутствует).

## Средние (можно решить по ходу Phase 1)
- **Q2. Telegram→Я.Телемост:** id `telegram` (способ интервью) используется в нескольких
  местах (label-map, select связи, сохранённые данные вакансий). Убрать совсем или
  заменить на `telemost`? Что делать с вакансиями, где telegram уже выбран?
- **Q3. «Документы» (блок 8 анкеты)** — Юрий забыл назначение. Прояснить, оставляем ли.
- **Q4. Глобальная страница «Кандидаты»** — роль/назначение (агрегат всех vs дубль
  per-vacancy)? Пока работает скоупом по компании.

## Отложенные (Юрий: «пока оставим» / позже)
- **Q5. Скоринг-движок** (v1/v2, v2 на входящем потоке) — НЕ трогаем сейчас (D4).
- **Q6. Conversational-анкета AI-бота** — финальная фаза (D2).
- **Q7. «Все» вид календаря** (слои Компания/HR/Мой в одной сетке) — нужен ли; + логика
  cross-calendar конфликтов (сейчас проверяются только переговорные).

## Подтверждения перед удалением (Phase 5)
- **Q8.** Перед удалением legacy-таба «Воронка» и мёртвых полей (unacceptableSkills,
  FAQ-в-рантайме, старый параметр задержки) — финальное подтверждение после стабильных
  Phase 3-4.

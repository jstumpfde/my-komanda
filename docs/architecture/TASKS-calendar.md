# TASKS — страница «Календарь» (/hr/calendar)

> Календарь интервью/событий найма. Код: `components/calendar/*`, `app/(modules)/hr/calendar/page.tsx`.
> Статусы: ✅ сделано · ⏳ в плане · ❓ ждёт Юрия.

## Сделано
- **C1. B1: «+ Новое событие» крашил страницу** ✅ (f2fbc584) — пустой `value` в Radix
  SelectItem «Без переговорной» → sentinel "none" (event-modal.tsx).
- **C2. Слот по умолчанию 30→45 мин** ✅ (d773dca2).

## В плане
- **C3. «Все» вид календаря** ❓ (Q7) — слои Компания / HR / Мой в одной сетке. Нужен ли?
  Если да — переключатель слоёв + цветовая легенда.
- **C4. Cross-calendar конфликты** ⏳ — сейчас проверяются только переговорные (комнаты).
  Нужно: проверять занятость HR/участников по их событиям при назначении встречи.
- **C5. Интеграции календарей (Google/Outlook/Яндекс/Zoom)** — заглушки «Скоро»
  (в hiring-settings, см. TASKS-hiring-settings I-блоки). Двусторонняя синхронизация —
  крупная фича, после CRM/мульти-hh.
- **C6. Напоминания (24ч/2ч)** — настройка есть (hiring-settings); проверить, что cron
  реально шлёт push/email кандидату и HR.

## Не баги (решено)
- Расписание настраивается в 3 местах (события / окна отправки / дефолты слотов) —
  это 3 разных хранилища, НЕ дубль.

## Зависимости
- C5 (синхронизация календарей) ↔ OAuth-интеграции (как hh) ↔ платформенный уровень (P1).

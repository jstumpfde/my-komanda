# TASKS — страница «Резерв» (/hr/talent-pool, бывш. Talent Pool)

> База пассивных кандидатов + прогрев. Код: `app/(modules)/hr/talent-pool/page.tsx`,
> `components/talent-pool/*`, API `app/api/modules/hr/talent-pool/*`.
> Статусы: ✅ сделано · ⏳ в плане · ❓ ждёт Юрия.

## Сделано
- **R1. Переименование «Talent Pool» → «Резерв»** ✅ (QW3, 8a763f6c) — весь UI платформы;
  роуты/ключи (`talent_pool`, `/hr/talent-pool`) и лендинг НЕ трогали.
- Стадия `talent_pool` в воронке уже звалась «Резерв» (lib/stages.ts) — консистентно.

## В плане / проверить
- **R2. Реальность данных** — таб «База» ✅ ПОДКЛЮЧЕН (2feed7cc): новый эндпоинт
  GET /api/modules/hr/talent-pool/candidates тянет реальных кандидатов стадии talent_pool.
  Имя/источник/скоринг/контакты — реальные; статус из скоринга; должность «—» (нет поля).
  Демо убрано, проверено в проде (резерв сейчас пуст → «Нет кандидатов»).
  - ⏳ Осталось по Базе: add/delete сохранять в БД (сейчас локально); поля должность/компания
    (нужны на candidate или talent_pool_entries).
  - ❌ Кампании / Рефералы / Аналитика / Формы — ВСЁ ЕЩЁ MOCK. Им нужны НОВЫЕ ТАБЛИЦЫ:
    `talent_campaigns` (прогрев), `referral_links` (рефералы), формы. + миграции + эндпоинты.
    Это полноценный модуль на несколько заходов. Кампании = outward (касания кандидатам).
- **R3. Прогрев пассивных кандидатов** ⏳ — кампании прогрева (есть UI «кампании»):
  проверить, реально ли отправляются касания, или это заглушка (как webhooks были).
- **R4. Форма-конструктор «Общей анкеты резерва»** (`form-constructor.tsx`) +
  tracking/referral-ссылки — проверить, сохраняются ли и собирают ли кандидатов.
- **R5. AI-скоринг резерва** (`talent-pool/match` route) — match резюме резерва ↔ открытая
  вакансия (радар «В резерве найдено N» в вакансии работает ✅, проверено).
- **R6. Кнопка «+ Добавить в резерв»** из карточки кандидата ✅ (диалог переименован, QW3).

## Приоритет
- Сначала R2/R3 (что реально работает vs mock/заглушка) — как с hiring-settings: отделить
  рабочее от обманок. Потом доводка прогрева (R3) — ⚠️ касания пишут живым кандидатам (outward).

## Зависимости
- R3 (прогрев) — outward (сообщения кандидатам) → доводка с Юрием.
- R5 (match) связан со скорингом вакансии (V2 в TASKS-vacancy-editor).

# Сессия 03.06.2026 (вечер→ночь) — автономная работа

> Юрий ушёл спать, дал мандат: «делай не спрашивая и не останавливаясь, утром
> задашь накопившиеся вопросы». Этот файл — что сделано + вопросы на утро.

## Состояние на конец сессии
- **develop HEAD = d5f1878c, прод (company24.pro) = d5f1878c** (синхронны). HTTP 200.
- Деплоев за сессию: **6**, каждый — `pnpm build` ✓ + `pm2 reload` (без простоя) + браузер-проверка.
- Тег отката доступен: `pre-consolidation-2026-06-03` (на f2fbc584).

## Сделано — всё в проде

### Фаза 1 — паритет Funnel Builder ✅ ЗАКРЫТА
- **Q1** (`7cffa54f`) — активная вакансия уважает явный `?tab=`.
- **T5** (`4dac9da6`) — сняты 5 ложных бейджей «Нужно для AI-скрининга».
- **T1** (`47c3127b`) — диагноз пустого Sheet: НЕ CSS-коллапс, а медленная загрузка
  блока «Анкета». Подписан лоадер «Загрузка настроек…».
- **T2/T3/T4** (`47c3127b`) — +3 блока (Тест / Хочет созвониться / Аварийное
  сообщение) → конструктор 20 блоков. Проверено в проде.

### Полигон «Помощник по маркетингу» (ИП Штумпф, id 5ae8f734…)
- **callIntent** блок включён в конфиге (отражает работающий legacy `callIntent.enabled`).
- **ai_resume_score** приведён к правде: реально скорит 1394/1505, но флаг+блок были
  «выкл» → поставил оба `true`. Рантайм не читает флаг (скорит всегда) → поведение не изменилось.
- ⚠️ Бэкап funnel_config до правок: на сервере `/tmp/funnel-bak-5ae8f734-b468-46fc-88f9-69ed662879ed.json`.
- Боевые вакансии Орлинка — НЕ трогал (по твоему указанию).

### Пачка QW/B (`8a763f6c`, `b5e11d60`)
- **QW3** Talent Pool → Резерв (весь UI; роуты/ключи/лендинг не трогал).
- **QW2** таб «Анкета» → «Вакансия» (ключ/анкета кандидата не тронуты).
- **QW10** плейсхолдеры orlink.ru → yourdomain.ru.
- **B7** FAQ resync, **B8** hiring-settings гидрация стоп-факторов.

### D12 — «Скоро»-заглушки скрыты от клиентов (`fce65c5c`, `d8bf0bb3`, `d5f1878c`)
Механизм: `isPlatformAdmin` из `/api/auth/me`; у клиента блоки `hidden`.
- Карточка вакансии: источники (Авито/SuperJob/Яндекс) + CRM (Битрикс24/AmoCRM/Другая).
- Настройки найма: опросы адаптации, интеграции календарей/Zoom, шаблоны сообщений.
- Брендинг: блок «Поддомен компании».

## ВОПРОСЫ НА УТРО (с моими рекомендациями)

1. **D12 на другие модули** (booking / dialer / b2b / qc / logistics / marketing /
   knowledge) — там целые страницы/модули-заглушки со «Скоро». Скрывать их от
   клиентов? Или это намеренный roadmap / уже управляется подпиской (module-access)?
   *Не трогал — вне фокуса «Найма» + риск ночью. Рек: скажи, какие модули — заглушки
   для скрытия, какие живые.*

2. **Чат-бот на полигоне** — твоя цель «общение/дожимы в чат-боте». Включить?
   Нужен промпт/сценарий. Это outward (бот пишет 1505 живым кандидатам на hh), промпт
   сейчас пуст. *Рек: дай промпт → обкатаем в песочнице (sandbox, Group 33) → потом включаем.*

3. **«Мёртвые» стоп-факторы/стоп-слова** на полигоне — заданы, но не применяются.
   Включить (заставить работать) или убрать как мусор? *Outward (авто-отказы) → нужен твой выбор.*

4. **IA-рефактор настроек** (D8, 4 раздела: Профиль/Компании · Интеграции · Дефолты
   вакансии · Служебные) — большой UX-блок, разблокирует QW5–11. Нужен дизайн «как
   выглядят 4 раздела» + маппинг настроек. *Рек: согласовать структуру до реализации.*

5. **Оставшиеся QW5–9, QW11** — мелкие UI. По твоей пометке «переедут с IA-рефактором».
   Делать сейчас или пачкой с D8? *Рек: с D8, иначе переделка.*

6. **Исходящий подбор (outbound, задача #4)** — ты показал ночью. Проверил: поиск +
   AI-скоринг + фильтр работают; недоделана **отправка приглашений** (`inviteResumeToVacancy`).
   Сверил с офиц. докой hh — текущий код пропускает обязательный шаг (применимые
   вакансии → `resulting_employer_state`). Разбор и план — в [docs/employer_negotiations.md](../employer_negotiations.md).
   Доводка нужна с твоим участием: финальный тест = реальное приглашение живому
   кандидату (outward). *Рек: утром добьём pre-шаг + сверим формат по OpenAPI + тест на 1 резюме.*

## Границы, которые держал ночью (намеренно НЕ трогал)
- Чат-бот / авто-отказы / рассылки — необратимо бьют по живым кандидатам.
- Боевые вакансии Орлинка.
- Массовая миграция funnel_config (отменена в пользу полигон-подхода — см.
  память `funnel-consolidation-scope-one-vacancy`).

# Готовый к выкату батч (26.06.2026, ночь)

Собрано за сессию, ждёт команды «кати». Деплой-команды (worktree/rm) в автономном режиме
отклонялись — поэтому НЕ выкачено, лежит в рабочем дереве `tmp/batch-pending`.

## Миграции (применять на прод-БД ДО сборки, по порядку)
- `0228_talent_pool_lifecycle.sql` — Резерв: колонки archived_at/trashed_at.
- `0229_default_schedule_mon_fri_1830.sql` — дефолт расписания Пн-Пт 09:00–18:30 (новые вакансии).
- `0230_offhours_autoreply_default_on.sql` — автоответ нерабочего времени ON по умолчанию (новые вакансии).

## Код (без новых миграций сверх вышеуказанных)
1. **Резерв — жизненный цикл** (архив/корзина/удаление): схема + entries PATCH + cron `talent-pool-cleanup` + retention API + UI (переключатель Активные/Архив/Корзина, popover «Хранение» + 152-ФЗ). После деплоя добавить crontab:
   `30 0 * * * curl -s -X POST -H "X-Cron-Secret: $CRON_SECRET" https://company24.pro/api/cron/talent-pool-cleanup >> /var/log/talent-pool-cleanup.log 2>&1`
2. **Расписание-дефолт** Пн-Пт 09:00–18:30: schema + can-send-now DEFAULTS/FALLBACK.
3. **Дефолты сообщений — на ВСЕ вакансии** (Юрий 26.06: старые истекли и не шлют → единый текст по всему порталу). `DEFAULT_INVITE_MESSAGE` (новое первое сообщение), `DEFAULT_OFF_HOURS_MESSAGE`, `DEFAULT_FIRST_MESSAGE_DELAY_SECONDS=300` в lib/hh/default-messages.ts. process-queue: DEMO_INVITE_MESSAGE=DEFAULT_INVITE_MESSAGE (дубль убран). Плюс новые вакансии при СОЗДАНИИ предзаполняются теми же текстами (firstMessagesChain+inviteMessage+offHours) — чтобы были видны/редактируемы в UI.
4. **Задержка msg1** (process-queue + first-messages-chain): defer свежего отклика; задержка = firstMessagesChain[0].delaySeconds или платформенный дефолт 5 мин. Применяется ко всем.
5. **Off-hours fallback** (process-queue): soft-mode + дефолт-текст когда свой пуст — на все вакансии с включённым off-hours.
6. **v2 hhStatus → hh-перевод** (Q1, flag-safe): changeNegotiationState +interview/+consider, sendNegotiationMessage (без смены стадии), hhActionForStatus (первичный контакт→invitation, тест→assessment, интервью→interview, отказ→discard), прошит в runtime-executor (5 мест).
7. **rubricScore убран из UI** (Q4): колонка скрыта по умолчанию + тумблер убран (код дремлет, данные целы).
8. **v2 ветки дожима** (Q3, flag-safe): модель `dozhimChainOpened` (ветка «открыл-не-досмотрел») + executor на 2 ветки + `switchV2BranchOpened` (отмена ветки А, план ветки Б при открытии демо) + триггер в mark-demo-opened + редактор второй ветки в funnel-v2-builder. ⚠️ НЕ протестировано вживую (на v2 нет кандидатов) — обкатать на пилоте РОП до включения.
9. **Фикс имени в v2-дожиме**: было `name.split(" ")[0]` (уходила фамилия) → безопасный `getCandidateFirstName`.
10. **Бэйдж «⚠ имя под вопросом»**: API считает `nameUncertain` (тот же резолвер, что отправка), прошит через хук/типы/Candidate → ⚠ у имени в списке (дефолт вкл, тултип). Серверная нагрузка — pure-функция, без доп. запросов.

## Уже ВЫКАЧЕНО вживую (не в батче — прямые правки БД, гейт был чист)
- Ветка Б дожима восстановлена на РОП/РП/Бизнес-ассистент Орлинка (9 текстов).
- Маркетолог (AI-платформа): первое сообщение (5 мин) + автоответ + inviteMessage-синк.

## Проверка после деплоя
- `cat .next/BUILD_ID` сменился; `curl https://company24.pro/login` = 200.
- Смоук: создать тестовую вакансию → проверить дефолт расписания Пн-Пт 09:00–18:30 + автоответ ON + первое сообщение = новый текст.
- Резерв: вкладки Активные/Архив/Корзина, действие «В архив».
- 0 свежих auth_failed в логах.

## Решения от Юрия (разблокируют дальнейшее, см. FUNNEL-V2-PARITY.md)
1. Маппинг hhStatus → hh-действие (A: 3 действия как legacy / B: русские этапы + мой маппинг).
2. Серия первых сообщений в v2 — нужна или хватает 1+дожим?
3. Ветки дожима (не открыл/открыл-не-досмотрел/тест) — переносить в v2 или упростить?
4. Выпил rubricScore из UI карточки (крон не крутится, токены не жжёт — осталась только UI-колонка).

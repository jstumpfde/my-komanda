# Аудит: страница вакансии (все вкладки) + /hr/hiring-settings

> Дата: 2026-05-30. Аудит по коду (каждая кнопка протрассирована до реального API-роута).
> Критерий статуса: ✅ реально сохраняет/грузит через API · ⚠️ работает, но баг/неполнота/UX · ❌ пустышка (toast/localStorage/мок/«Скоро»/dead code/404).

## Верхнеуровневые вкладки вакансии (6)
Порядок зависит от статуса. Active/published: **Кандидаты → Аналитика → Анкета → Демонстрация → Тест → Настройки**. Draft: Анкета первой, Настройки дефолтная.
`?tab=` значения: candidates / analytics / anketa / course (Демонстрация) / test / settings.

---

## 1. КАНДИДАТЫ (?tab=candidates)
- ✅ Режим «Список» — серверная пагинация `/api/modules/hr/candidates`.
- ⚠️ Канбан / Воронка / Плитки — рендерятся ТОЛЬКО для platform_admin. Обычные HR/клиенты их не видят (view-settings.tsx). Если так задумано — ок; иначе скрытый функционал.
- ✅ Фильтры (поиск, города, ЗП, источники, форматы, релокация, опыт, возраст, образование, языки, навыки, индустрии, демо-прогресс, даты, scoreMin/Resume/Anketa) — уходят на сервер.
- ✅ Сортировка по колонкам (состояние в URL).
- ❌ Дропдаун «Сортировка» (SortMenu) — обёрнут в `{false && …}`, недоступен (dead code).
- ✅ Пагинация (только список).
- ✅ Bulk-действия (`/api/modules/hr/candidates/bulk`): reject / invite / talent_pool / set_stage / toggle_favorite / restore.
- ✅ В строке: избранное, действия (advance/reject/reserve), открытие карточки.
- ✅ «Разобрать» / hh-синк (HhAutoProcess → process-queue), ручной hh-синк.
- ❌ **«Добавить кандидата»** — диалог есть, но НЕТ кнопки-триггера + сам диалог фейковый (random UUID/score, только локальный стейт, без API). Полностью мёртвый.
- ✅ Экспорт в Excel (`/export-candidates`).
- ✅ Диалог отказа (+ предложение Talent Pool). ⚠️ мелочь: при успехе показывает toast.error.
- ✅ Talent Pool радар (`/talent-pool/match`). ⚠️ «добавить в пул» = смена стадии, не отдельный реестр.
- ✅ Панель рубрики (RubricRankPanel → rubric-score-all, батч 50).
- ❌ **AI-инструменты (4 шт.): сравнение / вопросы интервью / reference check / генерация оффера** — хендлеры и модалки готовы, endpoint'ы существуют (`/api/ai/compare-candidates`, `/interview-questions`, `/reference-check-questions`, `/generate-offer`), НО из UI отрезаны: compare под `{false &&}`, остальные 3 не вызываются нигде. ~250 строк dead code с рабочим бэкендом.
- ✅ Настройки вида/карточки — persist через user-preferences (не localStorage).
- ✅ mark-seen, бейдж «+N новых», метрики шапки (`/stats` + `/candidate-stats`).
- ❌ `/api/funnel-stages` — роут НЕ существует, fetch всегда 404 → тихий фолбэк на COLUMN_ORDER (не фатально, но лишний 404 на каждом рендере; тот же вызов в hiring-settings).

## 2. АНКЕТА (?tab=anketa)
Сохранение/загрузка корректны (автосейв 2с + ручная кнопка, server-merge). Должность/мотивация/условия/обязанности/документы/hh-описание — ✅.
- ⚠️ **AI-профиль кандидата — путаница (жалоба Юрия).** Настройки для AI заданы в ДВУХ местах: секция «Портрет кандидата» (старый слой) и секция «AI-профиль» (новый) дублируют «обязательные навыки» и «стоп-факторы». В скоринг реально идёт новый слой (aiRequiredHardSkills, aiStopFactors, aiWeights, aiCustomCriteria), старый — фолбэк.
  - «Пустой критерий»: кнопка «+ Добавить» создаёт строку с пустым названием (на скоринг безопасно — игнорируется, но выглядит битым; нет автофокуса/подсказки).
  - Разница, которую надо развести: hard skills/стоп-факторы = жёсткие ФИЛЬТРЫ (нет → отсев/низкий балл); приоритеты оценки = ВЕСА баллов (вклад, не отсев). Сейчас свалено в один аккордеон.
  - Рекоменд: два подблока «🚫 Жёсткие требования» и «⚖️ Критерии оценки (баллы)»; убрать дублирование со старой секцией; автофокус+плейсхолдер на критерии; раскрыть секцию по умолчанию.
- ❌ Скрининг-вопросы — сохраняются, но НИКТО не читает (ни скоринг, ни чат-бот, ни анкета кандидата). Копятся вхолостую.
- ❌ Публичная секция анкеты (AnketaPublicSection) — нигде не смонтирована (комментарий обещает рендер, но его нет).
- ❌ AnketaTemplateControls / QuestionEditor — не подключены к вкладке.
- ⚠️ Поля-пустышки в типе без UI: showSalary, experienceMin/Ideal, конструктор вопросов, часть «Компания».
- ⚠️ Монолит 2652 строки — кандидат на декомпозицию.

## 3. ДЕМОНСТРАЦИЯ (?tab=course) и 4. ТЕСТ (?tab=test)
Один компонент CourseTab (kind demo/test) + NotionEditor.
- ✅ Авто-создание/загрузка/автосохранение (debounce 1.5с, `/api/modules/hr/demos`), сохранение на beforeunload (sendBeacon).
- ✅ Все блоки (текст, фото, видео, аудио, файл, инфо, кнопка, задание с 6 типами вопросов, запись медиа) — редактируются и сохраняются.
- ✅ Тулбар: вставка блоков, форматирование, скачать .txt, «создать с нуля».
- ⚠️ Кнопка «Сохранить» в тулбаре — фактически декоративная (`saveNow` только тост, реально сохраняет фоновый автосейв; может показать «Сохранено» раньше факта).
- ❌ **Шаблоны демо — в localStorage**, а не в БД (хотя таблица `demo_templates` и API существуют). Не шарятся между устройствами/командой, теряются при чистке браузера.
- ✅ AI-генерация демо (`/api/modules/hr/demo/generate`, двухэтапная, прогресс). ⚠️ перезаписывает контент (предупреждение только текстом).
- ✅ Загрузка файла DOCX/PDF/TXT → бьётся на уроки.
- ⚠️ Встроенный previewMode редактора недоступен на вкладках (метод есть, но не выведен в тулбар; доступно только косвенно через «Открыть как директор»).
- ✅ Прохождение ДЕМО кандидатом (`/demo/[token]`) — полное: все типы блоков, видео, запись медиа, AI-скоринг ответов.
- ❌ **Прохождение ТЕСТА примитивно** (`/test/[token]`, «Этап 1»): HR настраивает structured-вопросы (single/multiple/баллы), а кандидат видит ОДНО текстовое поле. Типы вопросов и автобаллы на стороне кандидата не работают.
- Отличие test от demo: один редактор, разный `kind`; у теста — своя упрощённая публичная страница + test_submissions + серверный скоринг + вердикт в карточке.
- ℹ️ Мусор рядом: course-tab.backup.tsx, route.backup.ts, неиспользуемые course-constructor.tsx / notion-course-tab.tsx — на удаление.

## 5. АНАЛИТИКА (?tab=analytics)
Серверного endpoint аналитики НЕТ — всё считается на клиенте из загруженных `columns`.
- ⚠️ «Всего кандидатов» / «Конверсия» / «Ср. AI-скор» / воронка / источники / распределение скора — считаются из ЧАСТИЧНО загруженной выборки. На вакансиях с пагинацией цифры занижены и расходятся с шапкой (которая берёт `/stats`).
- ⚠️ «Ср. AI-скор» и распределение используют поле `c.score`, а не реальный `aiScore` → искажение для непроскоренных.
- ⚠️ Фильтр «Период» непоследователен: метрики «Всего»/«Конверсия» и воронка его игнорируют (берут нефильтрованные данные), реагируют только источники и распределение.
- ✅ «Дней активна» (из createdAt) — единственный полностью корректный блок.
- Вывод: моков нет, но вся вкладка нуждается в серверной агрегации, иначе врёт на больших объёмах.

## 6. НАСТРОЙКИ (?tab=settings) — 9 под-табов
Почти всё реально сохраняется (у каждого свой рабочий роут, проверены все 25). Работают: Брендинг, Источники (hh/UTM/мини-форма/публичная страница), Воронка (преквалификация/стоп-факторы/стоп-слова/финальные экраны/post-demo), Конструктор воронки [Beta], Сообщения (серия первых/автоматизация/аварийное), Дожим (требования/AI-фильтр/цепочка), AI чат-бот, Расписание, бот-звонарь.
Проблемы:
- ❌ AI чат-бот → «Тест Telegram» → 404 (роут `ai-chatbot/test-telegram` не существует). Фикс: создать роут или перенаправить на `/company/telegram/test`.
- ❌ Интеграции (Битрикс24 / AmoCRM / Другая CRM) — вся вкладка `disabled` + «Скоро».
- ❌ Источники: Авито / SuperJob / Яндекс.Работа — `disabled` + «Скоро».
- ❌/⚠️ Брендинг → домены: «Проверить DNS» = toast «скоро»; subdomain/custom сохраняются, но реального роутинга по домену нет (ссылка ведёт на обычный /vacancy/[slug]).
- ⚠️ «Редактировать» вакансию делает грубый window.location.reload() вместо refetch.
- ⚠️ Баннеры «AI чат-бот отключит блоки на след. неделе» — плейсхолдер недоделанной фичи.
- ℹ️ Комментарий «стоп-факторы не подключены к process-queue» устаревший — они применяются (process-queue.ts:719).

## 7. КАРТОЧКА КАНДИДАТА (drawer, открывается из списка)
7 вкладок, грузит `/api/modules/hr/candidates/{id}`.
- ✅ Контакты, hh-резюме, источник/реферал.
- ✅ Заметки (`/notes`).
- ✅ Answers (анкета+демо, `/qualification`), видео-визитка.
- ✅ Chat — реальная переписка hh.ru (`/api/integrations/hh/messages/...`).
- ✅ AI (скоринг резюме aiScore/V2) + пересчёт (`/score-candidate`).
- ✅ Rubric (рубрика) + пересчёт (`/rubric-score`).
- ❌ **Channels (Telegram/WhatsApp/Email)** — чистая заглушка «в разработке», никаких данных.
- ✅ History (таймлайн стадий из stageHistory).
- ✅ Действия: смена стадии / отказ / восстановить / избранное / заметка / AI-скоринг / рубрика / сообщение в hh — все через реальные endpoint'ы.
- ℹ️ Минор: cityOptions/sourceOptions/allCandidates вычисляются, но не используются (dead code).

---

## /hr/hiring-settings («Дефолты компании») — в основном ФАСАД ⚠️
Реально работает (серверный API): Аварийное отключение AI (kill-switch), Режим строгости AI чат-бота, Темп отправки, Корзина-срок хранения, вкладка «Интеграции», тумблер «Выбор компании» (localStorage by design).
Пустышки (НЕ сохраняются — localStorage/toast):
- ❌ Расписание (слоты/часы/дни/напоминания/способы интервью/часовой пояс) — «Сохранить» = только toast.
- ❌ Стоп-факторы (дефолты) — «Сохранить» = только toast.
- ❌ Webhooks — localStorage.
- ❌ Битрикс24 — localStorage.
- ❌ Хранение данных (ФЗ-152) — localStorage, не применяется.
- ⚠️ Воронка — «Сохранить» пишет в ГЛОБАЛЬНУЮ funnel_stages (не «дефолт для новых вакансий»); блок «Автоматизация» (autoDemo/autoInvite/minScore/autoReject) не сохраняется.
- ❌ «Скоро»-заглушки: Шаблоны сообщений, Сбор обратной связи 30/60/90, Интеграции календарей (Google/Outlook/Яндекс/Zoom).

---

## ИТОГ — приоритеты
1. **hiring-settings: честность страницы.** HR жмёт «Сохранить», видит ✅ — но Расписание/Стоп-факторы-дефолты/Воронка-автоматизация/Webhooks/Битрикс/Хранение никуда не пишутся. Либо допилить (per-company API + применение при создании вакансии), либо закрыть «Скоро». Самый опасный класс — тихая ложь.
2. **AI-профиль в анкете** — развести фильтры и критерии-баллы + убрать дублирование (ежедневный экран, прямая жалоба).
3. **Аналитика** — серверная агрегация (сейчас врёт на больших вакансиях; период работает непоследовательно).
4. **Прохождение Теста** — кандидат видит одно поле вместо настроенных вопросов; либо доделать, либо убрать богатый редактор теста.
5. **Шаблоны демо** — перенести с localStorage в существующую таблицу demo_templates.
6. **Dead code с рабочим бэкендом** — 4 AI-инструмента в списке кандидатов + «Добавить кандидата»: вернуть в UI или удалить.
7. **Мелочи**: «Тест Telegram» 404; `/api/funnel-stages` 404; Channels-заглушка в drawer; reload при редактировании; toast.error при успешном отказе; мусорные .backup файлы.
</content>
</invoke>

# Сессия 27.04.2026 — итоговый отчёт

**34 деплоя за день.** Ветка `feature/candidate-media-recording`.

## Итоговое состояние v27.04-toggle-and-checks
- Тег: ещё не поставлен — текущий HEAD (PM2 #2857)
- Git log топ-5:
  - WIP: enabled toggle + spacing + galочки убраны (несомм.)
  - e16b637 farewell + brandName + логотип на форме
  - 6dbdd8d post-demo extras (button toggle, formFields)
  - 9eb1d09 demo fixes (ФЗ-152 + defensive parsing)
  - 170c815 post-demo settings БД+API+UI

## Что сделано (от старта до конца дня)
1. Хвосты воронки (3 фикса): no-duplicates fallback, отключение мёртвой hh-интеграции, AI advisor через proxy + autoStop при reject
2. Рефакторинг Плиток: отдельный компонент DemoProgressBar и AiScoreBadge, убраны пустые иконки, бейджи источника
3. Демо-страница для кандидатов:
   - Светлые стили всей формы (autofill, чекбоксы, native input color-scheme)
   - Запись 3-5 минут добавлены в селект
   - Кнопка «Посмотреть как кандидат» в админке
4. Post-demo settings (полная цепочка):
   - БД: demos.post_demo_settings jsonb
   - API: GET/PUT /post-demo-settings
   - Админка: компонент PostDemoSettings (manual/auto + thresholds + 4 шаблона блока)
   - Public: рендер блока по mode/score/thresholds, replaceName с firstName
5. Финал-экран отполирован:
   - Галочки в кружочках убраны (промежуточная форма + финал)
   - Логотип большой сверху (h-16 для финала, h-12 для формы)
   - Название компании из brandName с fallback на companies.name
   - Прощальный экран «До скорой встречи!» после клика «Хорошо, жду!»
   - Тумблер «Включено/Выключено» в админке (но логика на public/demo не реализована)
   - Карта «Финальная анкета» с настройкой 7 полей (показывать/обязательное)
   - Отступ между секциями автоматизации

## Pending
1. **Логика `enabled === false`** на public/demo — тумблер сохраняется, но публичная страница не реагирует
2. Дублирование функционала AI-обработка vs Авто-действия — разобраться
3. Слипшиеся блоки в табе «Автоматизация» — нормальные отступы между всеми секциями
4. Финальная форма — добавлять свои поля (сейчас только включать/выключать существующие 7)
5. **Боевой прогон 50 кандидатов** на вакансию `3e8d1f6b-b3bf-4f71-8d77-85a3a9344d71`

# HANDOFF — сессия 31.05.2026 (для нового чата)

> Локальный файл, НЕ коммитить. Юрий — фаундер, не программист.
> Правила работы: писать «РЕКОМЕНДУЮ» в предложениях; ETA по шагам; объяснять
> по-человечески без жаргона; не задавать лишних вопросов — идти по плану.

## ⚠️ ПРАВИЛА ДЕПЛОЯ (КРИТИЧНО — Юрий на это ругался)
1. **`git push` делает ТОЛЬКО ассистент со своего Mac** по команде Юрия «пушь».
   На прод-сервере push заблокирован хуком (правильно). Если дать Юрию строку
   с `git push` — он вставит её на сервере и получит ошибку.
2. **Команды Юрию давать ПО ОДНОЙ.** Не слать блоком, не слать в markdown-блоке
   то, что он НЕ должен вставлять (push — это работа ассистента). Дал команду →
   подождал результат → дал следующую.
3. На сервере Юрий выполняет только: `cd /var/www/my-komanda && git pull origin
   develop && pnpm build && pm2 reload my-komanda` (одной строкой — безопасно).
   Если миграции — отдельно psql ДО reload.
4. Длинные build-warning про `public/uploads/... matches 11xxx files` —
   НЕ ошибки, давний хлам (path.join в save-candidate-photo.ts / upload routes /
   upload-media). Сборка всегда `✓ Compiled successfully`. Не пугаться.

## ✅ ЧТО СДЕЛАНО ЗА СЕССИЮ (всё на проде develop, запушено, задеплоено)
Коммиты (новые сверху):
- c2bb5ba — анкета: структурированные языки+уровни и образование + подтяжка в фильтр
- a1bb6aa — outbound: кнопка «Заполнить из анкеты»
- 4cbf1d3 — outbound: расширенный фильтр поиска (как на hh: занятость/график/
  образование/язык/возраст/пол/переезд/сортировка/метки)
- 7e330a2 — анкета: подсказка про число обязательных навыков (жёлтое при >8)
- 6b6357a — outbound: фикс города → hh area id (был 400 bad_argument)
- 2a7d0bd — МОДУЛЬ «Исходящий подбор» hh Фаза 1 (Заход 9)
- f319eae — Telegram-фундамент данных (Заход 8.0)
- ef5a5e5 — серверная аналитика вакансии (Заход 7)
- 4a2437c — прохождение Теста со структурированными вопросами (Заход 6)
- ab4a768 — чистка: фейковый «Добавить кандидата» + backup-файлы (Заход 5)
- 3f515c7 — применение дефолтов компании при создании вакансии (Заход 4)
- 9000c8a — честная персистентность дефолтов компании /hr/hiring-settings (Заход 3)
- d5a113e — анкета: AI-профиль разведён на «фильтры»/«критерии-баллы» + UX пустого критерия
- e8b9e1b — фикс «Тест Telegram» 404 + красный тост на успешном отказе (Заход 1)
- 455133e — инфраструктура отложенных отказов (Заход 2)

Миграции на проде: 0155 (отложенные отказы), 0156 (hiring_defaults), 0157
(test answers_json), 0158 (telegram-фундамент), 0159 (outbound 3 таблицы).
Крон на проде: `*/5 * * * * pending-rejections` — работает ({"ok":true,...}).

## ⏳ ЧТО ОСТАЛОСЬ / ХВОСТЫ
1. **Юрий должен УРЕЗАТЬ в Анкете 28 «обязательных навыков» до 4–6 ключевых**
   (это ДАННЫЕ вакансии, правит сам руками). Сейчас их 28 вперемешку →
   AI-баллы размыты, не различают кандидатов. КРИТИЧНО для качества подбора.
   Принцип скоринга: совпадение по доле (не нужны ВСЕ навыки); в ноль валят
   только «стоп-факторы» (у него пустые). Объяснено, подсказку в UI добавили.
2. **Кнопку «Пригласить» в Исходящем подборе на РЕАЛЬНЫХ резюме НЕ жать** —
   формат hh negotiations не сверён с докой (TODO в lib/hh/outbound.ts
   inviteResumeToVacancy + checkResumeDatabaseAccess эвристика). Поиск+скоринг
   работают независимо. ПЕРЕД боевым «Пригласить» — сверить docs/employer_negotiations.md.
3. **Telegram чат-бот для КАНДИДАТОВ (фазы 1+)** — только фундамент (0158:
   candidates.telegram_chat_id, companies.candidate_bot_token, ai_chatbot_messages.channel).
   Вебхука/бота/UI ещё НЕТ. Юрий хочет — общение бота с кандидатами в Telegram.
   Когда возьмёмся: Юрий создаёт бота в @BotFather (/newbot → токен), ассистент
   делает: поле токена в ЛК + webhook candidate-webhook/[companyId] + матчинг по
   /start <candidates.token> + прогон processChatbotMessage (он КАНАЛО-НЕЗАВИСИМ,
   менять не надо) + отправка ответа. Фаза 1 рекоменд: dryRun-эхо без записи в БД.
   Бот для HR-эскалаций ≠ бот для кандидатов (НЕ путать, разные токены).
4. **Смена CRON_SECRET** — засветился в чате (49b9fe016c6bb9c625053d3b33fb08f8199a7a6b88a00191a4a933f10dd9cabd).
   Сменить: openssl rand -hex 32 → вписать в .env.local CRON_SECRET= → pm2 reload
   --update-env → заменить во ВСЕХ 5 строках crontab → проверить curl. Не срочно.
5. **Заход 3 старого плана (отложенные отказы)** — инфраструктура есть и крон
   крутится, НО точки мгновенного отказа ещё НЕ переключены на scheduleRejection.
   Карта 6 точек — в HANDOFF-scoring-rejections.md. Сейчас отложенный механизм
   работает вхолостую, мгновенные отказы уходят как раньше.
6. **Автоматизация воронки в hiring-defaults (Заход 3)** — сохраняется, но при
   создании вакансии НЕ применяется (нет однозначного маппинга на поля вакансии).
7. **Язык/образование в анкете** — ДОБАВЛЕНЫ (c2bb5ba). Юрий должен проверить в
   браузере: Анкета → Портрет кандидата → поля «Уровень образования»/«Языки»;
   Исходящий подбор → Расширенный фильтр → кнопка «Заполнить из анкеты».

## 📋 ПРОВЕРИТЬ ГЛАЗАМИ (кандидато-видимое, ассистент в браузере не гонял)
- Анкета: новый AI-профиль (2 блока), языки/образование.
- Прохождение Теста со структурированными вопросами (/test/[token]).
- Исходящий подбор: поиск по Москве + AI-скоринг + расширенный фильтр + кнопка.

## 📄 ДОКУМЕНТЫ В КОРНЕ
- AUDIT-vacancy-and-hiring-settings.md — полный аудит всех вкладок вакансии + hiring-settings.
- SYSTEM-MAP.md — общая карта как всё работает (создаётся этой сессией).
- HANDOFF-scoring-rejections.md — контекст отложенных отказов (Заходы 1-4 старого плана).
- TZ-OUTBOUND-SOURCING (1).md в ~/Downloads — ТЗ исходящего подбора (Фазы 2-3 не сделаны).
</content>
</invoke>

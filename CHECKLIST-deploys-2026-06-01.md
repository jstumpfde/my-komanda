# Чеклист деплоев — состояние на 01.06.2026

**Главное:** прод-сервер `/var/www/my-komanda` крутит ветку **develop**, сегодня
обновлён до `a63e000` (build + pm2 reload сделаны). Весь список ниже **физически
задеплоен**. Вопрос только: применена ли миграция и проверял ли ты руками.

Ветка `main` отстала от develop на ~40 коммитов и как «прод-ветка» де-факто НЕ
используется. См. раздел «Риски» внизу.

**Миграции подтверждены применёнными на проде (01.06.2026):** 0116 (cron_runs),
0144 (test_submissions), 0155+0160 (отложенный отказ — колонки на `candidates`),
0156 (hiring_defaults), 0157 (test answers_json), 0159 (outbound). Риска по
миграциям нет.

Легенда статуса:
- ✅ задеплоено + проверено в этой сессии
- 🟡 задеплоено, **тобой не проверено** — нужна твоя проверка
- ⚠️ задеплоено, но **есть незаконченный хвост** (фича не доведена)
- 🗃️ зависит от миграции — подтвердить запросом (см. README выше)

---

## A. Проверено в этой сессии — закрыто

| # | Что | Коммит | Как убедиться |
|---|-----|--------|---------------|
| A1 | hh-import: lock держится ~4с (был ~100с) + лог в cron_runs | `a63e000` | ✅ cron_runs: duration_ms ~4000, прогон раз в минуту |
| A2 | hh: интеграция НЕ деактивируется при сбое/гонке рефреша токена | `4c398b8`,`6bbc53b` | ✅ Орлинк восстановлен, токен валиден до 2026-06-15 |
| A3 | «Синк» показывает реальное время cron-импорта | `9325dc7` | ✅ |
| A4 | Дожим останавливается при paused-вакансии | `c0e4517` | ✅ логика + все дожимы отменены (обратимо) |
| A5 | Аналитика вакансии — контент на всю ширину (убран max-w) | `69ffc61` | ✅ |

---

## B. Проверка глазами — результат (от Юрия 01.06.2026)

| # | Что | Статус |
|---|-----|--------|
| B1 | Дублирование вакансии копирует ВСЕ настройки | На странице анкеты — ✅ работает. В **списке вакансий** кнопка «Дублировать» звала обрезанный эндпоинт → **исправлено** (commit pending): теперь зовёт `/[id]/duplicate`. Нужен деплой + проверка. |
| B2 | Анкета: «О компании», блок времени публикации, дедуп навыков | ✅ работает |
| B3 | best-publish-time больше не даёт 500 | ℹ️ это и есть блок «рекомендованное время публикации на hh» (B2). Отдельной проверки не требует — техфикс 500-й ошибки эндпоинта под этим блоком. |
| B4 | Панель массовых действий над вакансиями | ✅ есть |
| B5 | Дропдаун «Ещё» + панель «Соответствие · рубрика» в списке кандидатов | ✅ на месте. **→ В очередь:** будут доп. задачи по этому блоку (см. ниже). |
| B6 | Таб «Рубрика» в карточке кандидата (оценка по критериям анкеты) | ✅ на месте. **→ В очередь:** задача по «очереди оценки» (см. ниже). |

---

## C. Задеплоено, но с незаконченным хвостом ⚠️ (это и есть твои задачи #1 и #4)

| # | Что | Коммит / миграция | Что НЕ доделано |
|---|-----|-------------------|-----------------|
| C1 | **Тест кандидата** (структурир. вопросы) — задача #1 | `4a2437c` (0144,0157 ✅) | Тест-стадии НЕ в `ADVANCED_STAGES` (дожим по тесту не останавливается); тест-стадии в UI-воронке (просмотр/редактор) + маппинг на hh-стадии; механика рассылки теста |
| C2 | **Исходящий подбор** hh — задача #4 | `2a7d0bd`,`4cbf1d3`,`a1bb6aa`,`c2bb5ba` (0159 ✅) | «Чтобы работал корректно» — отдельная задача на доводку |
| C3 | Заход 3: отказ через отложенный механизм (стоп-факторы + провал предкв) | `95b496f` (0155,0160 ✅) | Осталось 2 точки разговорного отказа (scan-incoming, чат-бот) |
| C4 | 3 AI-инструмента в карточке кандидата (вопросы/реф-чек/оффер) | — | Хендлеры есть, но НЕ подключены кнопки-триггеры в CandidateDrawer |
| C5 | Telegram-канал чат-бота | `f319eae` / 🗃️0136,0158 | Только фундамент данных, не запущено |

---

## D. Инфраструктурные хвосты

- ⚠️ **CRON_SECRET засветился** в чате (в crontab) — ротировать секрет.
- ⚠️ **main отстал** на ~40 коммитов. Если кто-то задеплоит с main — все фичи
  пропадут. Решение: либо формально зафиксировать «прод = develop», либо
  фаст-форвард `main` до `develop`.
- TODO из CLAUDE.md: удалить старые ветки `deploy/pagination-clean`,
  `feat/pagination-v1`; перевести стейджинг на develop.

---

## Рекомендуемый порядок (мой)

1. **Сначала #3 (этот чеклист)** — прогнать запрос миграций, проставить статусы.
2. **Потом #1 (запуск тестов)** — но С1 имеет хвосты: перед рассылкой живым
   кандидатам убедиться, что тест-стадия не ломает дожим и корректно мапится.
3. **#4 (исходящий поиск)** — отдельная сессия на доводку.
4. **#2 (пройти все воронки по FLOW-документу)** — финальная сверка.

---

## Очередь задач (накопитель — детали уточняются)

| # | Задача | Откуда | Статус |
|---|--------|--------|--------|
| Q1 | Список вакансий: «Дублировать» зовёт полный эндпоинт `/[id]/duplicate` | B1 | ✅ исправлено (commit `cc2cc88`), ждёт пуша+деплоя+проверки |
| Q2 | Блок «Кандидаты / Соответствие · рубрика» (список + дропдаун «Ещё») — доп. задачи | B5, Юрий | 🅿️ ждёт деталей от Юрия |
| Q3 | «Очередь оценки» — таб «Рубрика» в карточке кандидата (оценка по критериям) | B6, Юрий | 🅿️ ждёт деталей от Юрия |
| #1 | Тест кандидата: конвейер рассылки (`{{test_link}}`, массовая отправка, стоп дожима) | роадмап | ✅ ядро собрано (commit `0dbc270`), ждёт пуша+деплоя+теста. Хвост: HR-редактируемый текст приглашения (сейчас дефолт в коде) |
| #2 | Пройти все воронки по FLOW-документу | роадмап | 🅿️ |
| #4 | Исходящий подбор — довести до корректной работы | роадмап | 🅿️ |
| D1 | Ротировать засвеченный `CRON_SECRET` | инфра | 🅿️ |
| D2 | Решить судьбу ветки `main` (прод=develop или ff main→develop) | инфра | 🅿️ |

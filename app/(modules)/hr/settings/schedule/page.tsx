import { redirect } from "next/navigation"

// Мёртвая страница-дубль удалена 11.07 (разведка по задаче #6 «единый центр
// настроек расписания»): здесь был локальный useState-грид «рабочих часов»,
// чья кнопка «Сохранить» показывала toast, но НЕ делала ни одного запроса —
// пользователь считал расписание сохранённым, а оно никуда не писалось.
// Рабочие настройки календаря/записи: /hr/calendar (шестерёнка) и
// /hr/hiring-settings?tab=interview («Часы для записи кандидатов»).
export default function HrSchedulePage() {
  redirect("/hr/calendar")
}

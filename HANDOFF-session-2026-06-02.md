# Передача дел — сессия 02.06.2026

Контекст почти полон (92%), закрываем чат. Ниже — что сделано, что в проде, что
ждёт деплоя и что осталось. Всё в ветке `develop`.

---

## 1. ДЕПЛОЙ — что в проде, что ждёт

Юрий деплоит сам с сервера. Стандарт (мягкий, без простоя):
```bash
cd /var/www/my-komanda && git pull origin develop \
  && [если в пачке есть drizzle/NNNN_*.sql → sudo -u postgres psql -d mykomanda -f drizzle/NNNN_*.sql] \
  && pnpm build && pm2 reload my-komanda --update-env
```
ПРАВИЛА: всегда `reload` (НЕ `restart`); НИКОГДА `rm -rf .next` на живом проде в
рабочее время. Миграцию всегда вкладывать в ОДНУ строку деплоя (Юрий просто
выполняет, что дают).

**Миграции уже выполнены на проде:** `0162_candidate_soft_delete.sql`,
`0163_compare_shares.sql`.

**⚠️ Ждёт обычного деплоя (без миграций)** — последний подтверждённый деплой был
на `1e89191`. После него НЕ задеплоено:
- `acd93c6`, `7123eb6` — квадраты прогресса демо + зазор;
- `098f183` — режим «Тест» (таблица из меню «Вид») + эндпоинт `test-table`;
- `b75a90f` — лимит сравнения 12→50 + выравнивание шапок.
Один мягкий деплой подтянет всё. **Проверить первым делом в новом чате: задеплоила ли.**

---

## 2. ЧТО СДЕЛАНО В ЭТУ СЕССИЮ (всё в develop)

**Тест/дожим:**
- Дедуп приглашений на тест по hh-чату (`lib/messaging/test-invite.ts`), не только candidate_id.
- Колонка «Тест» показывает «ошибка» при провале отправки (не ложное «отп.»).
- Публичный доступ `/test/`, `/candidate-update/`, `/compare/` — добавлены в
  `PUBLIC_PREFIXES` (`middleware.ts`). ⚠️ ГРАББЛИ: новые страницы в `app/(public)/`
  НАДО вносить туда вручную, иначе редирект на /login (был инцидент).

**Сравнение кандидатов (главная фича сессии):**
- Страница `/hr/vacancies/[id]/compare?ids=...` — матрица «вопросы × кандидаты» +
  режим «По вопросам». Источник данных — `lib/compare/build-comparison.ts` (общий хелпер).
- Кнопка «Сравнить» в нижней панели массовых действий (≥2 выделенных).
- Секции Тест/Демонстрация/Анкета, сворачиваемые.
- Действия по кандидату в шапке: ⭐/пригласить/отказать/открыть карточку/удалить/убрать.
- «Скачать Excel» → серверный .xlsx (`compare/export`).
- «Поделиться» → публичная ссылка `company24.pro/compare/{token}` (7 дней, таблица
  `compare_shares`, отзыв через revoked_at — кнопки отзыва в UI пока нет).
- Ответы: `|||` → маркированный список; демо/анкета JSON `{"q-..":"text"}`/`{}` →
  чистый текст; строка скоров «резюме | демо% | тест (got/max) | AI».
- Лимит сравнения 50 (был 12).
- AI-модалка «Сравнить топ» (`/api/ai/compare-candidates`) теперь учитывает тест+демо.

**Режим «Тест»** (`098f183`): меню «Вид» → пункт «Тест — ответы кандидатов» →
страница `/hr/vacancies/[id]/test-table` (строки кандидаты, колонки вопросы).

**Кандидаты — корзина:** мягкое удаление (`candidates.deleted_at`, миграция 0162).
Bulk API `trash/untrash/hard_delete`, права только admin/менеджер-платформы/директор.
Список и счётчики шапки фильтруют `deleted_at`. `?trashed=true` отдаёт корзину.
UI-вход в корзину (тумблер + восстановление) — НЕ доделан (см. §4).

**Команда (`/settings/team`):** список тянется из реальной БД (`/api/team`), не демо.
Постоянная редактируемая ссылка-приглашение = `companies.join_code` (уникальная,
без срока), правится + тумблер активности; `/api/companies` PUT принимает
`join_code`/`join_enabled`.

**Прочее:** авто-сохранение стоп-слов и ключевых слов созвона; «не актуально» =
отказ в классификаторе; квадраты прогресса демо.

---

## 3. ОТКРЫТЫЕ ХВОСТЫ ОТ ПОСЛЕДНЕГО СООБЩЕНИЯ ЮРИЯ (по странице сравнения)

1. ✅ Лимит 12→50 — сделано (`b75a90f`, ждёт деплоя).
2. ⏳ **«Открыть карточку» → боковая карточка (drawer), как в списке кандидатов**, а не
   «сырая» страница `/hr/candidates/[id]`. НЕ сделано. Нужно: вынести `candidate-drawer`
   (из списка) так, чтобы открывался поверх страницы сравнения. Юрий подтверждал интерес.
3. ✅ Выравнивание шапок (align-top + иконки mt-auto) — сделано (`b75a90f`).
4. ❓ **Пункт #4 у Юрия обрезался — СПРОСИТЬ, что там было.**

---

## 4. ОТЛОЖЕНО НА «ПОСЛЕ ПЕРЕЕЗДА» (см. также TODO-after-migration.md)

- **Zero-downtime деплой** — подготовлено опт-ин (`next.config.mjs` флаг
  `NEXT_OUTPUT_STANDALONE=1` + `ecosystem.config.cjs` cluster + runbook
  **`DEPLOY-zero-downtime.md`**). Прод НЕ затронут. Активировать: обкатать на
  стейджинге (new.company24.pro), потом cutover. Каверзы: env standalone,
  рантайм-uploads через симлинк.
- **Корзина кандидатов — UI**: тумблер «Корзина» у фильтров + восстановление в
  один клик (бэкенд готов: `?trashed=true`, untrash). + фильтр deleted_at в
  остальных счётчиках (воронка/дашборд/экспорт) + авто-очистка по trash_retention_days.
- **/privacy, /terms** (и about/contact/changelog) — добавить в PUBLIC_PREFIXES
  (сейчас аноним улетает на /login).

---

## 5. ПЕРЕЕЗД / ПОЛЬЗОВАТЕЛИ

Скоро переезд (сервер/БД). Контекст:
- Пароли — bcryptjs-хэш, в открытом виде НЕ существуют. При переносе той же БД
  логины переедут сами. Команды сброса пароля (если нужно зайти под кем-то) —
  `node -e "require('bcryptjs').hashSync('пароль',10)"` → UPDATE users.password_hash.
- Орлинк (company.title ~ %рлинк%, vacancy РОП id `58ba1d88-73b4-4f1e-b2f0-959e47bf6c60`):
  2 пользователя — `k.safronova@orlink.ru` (director), `jstumpf@ya.by` (hr_manager).
- Тест по РОП: 150 получили, 9 нет (1 ретраибельный hh_502 — Иванов, можно дослать;
  8 — resume_not_found/no_hh_link, недоставляемы).

---

## 6. КЛЮЧЕВЫЕ ФАЙЛЫ

- `lib/compare/build-comparison.ts` — сборка данных сравнения (общая для HR/публ/AI/экспорт/test-table).
- `app/(modules)/hr/vacancies/[id]/compare/page.tsx` — страница сравнения (HR).
- `app/(public)/compare/[token]/page.tsx` — публичная (read-only) версия.
- `app/(modules)/hr/vacancies/[id]/test-table/page.tsx` — режим «Тест».
- `components/dashboard/view-settings.tsx` — меню «Вид» (+ пункт «Тест» через `testTableHref`).
- `components/dashboard/bulk-actions-bar.tsx` — нижняя панель (Сравнить/Удалить/…).
- `components/hr/demo-progress-bar.tsx` — квадраты прогресса.
- `app/(platform)/settings/team/page.tsx` — Команда + ссылка-приглашение.
- `middleware.ts` — PUBLIC_PREFIXES.
- `app/api/modules/hr/candidates/bulk/route.ts` — bulk (trash/untrash/hard_delete, дедуп hh-чат при тест-рассылке в test-invite.ts).

CLAUDE.md и память (`...memory/`) актуальны: B3-дубли + дедуп теста, public-routes-middleware-whitelist, working-mode и пр.

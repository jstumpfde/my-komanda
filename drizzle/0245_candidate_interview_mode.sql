-- Вид интервью per-кандидат (Юрий 03.07): Звонок | Онлайн | В офис.
-- HR выбирает вид при приглашении (диалог/галочка/мастер рассылки) — страница
-- самозаписи кандидата показывает календарь этого вида. NULL = вид из воронки
-- (interviewMode интервью-стадии) / дефолта компании, как раньше.
ALTER TABLE candidates ADD COLUMN IF NOT EXISTS interview_mode text;

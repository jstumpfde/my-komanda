-- Согласие на обработку ПД (152-ФЗ) для заявок лендинга/портфолио (Юрий 08.07):
-- ранее чекбокс согласия был только клиентским гейтом кнопки, нигде не
-- персистился. Аддитивная nullable-колонка — NULL для старых записей и для
-- будущих типов интереса, где согласие не спрашивается.
ALTER TABLE landing_leads ADD COLUMN IF NOT EXISTS consent_at timestamptz;

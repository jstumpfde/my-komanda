-- Интервью-модуль: полный статус интервью (5 значений) отдельно от статуса
-- события календаря (3 значения). Позволяет хранить «Пройдено»/«Не явился»,
-- которые в calendar_events.status не выражаются. NULL = выводим из event.status+время.
ALTER TABLE calendar_events
  ADD COLUMN IF NOT EXISTS interview_status text;  -- Подтверждено|Ожидает|Пройдено|Не явился|Отменено

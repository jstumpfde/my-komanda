-- Единый таймстамп «последней активности кандидата» для фильтра «активны сейчас».
-- Дёргается при ответе в демо и при автосохранении/открытии теста. По нему
-- считаем isActive (активность за последние 30 минут) — и демо, и тест.
ALTER TABLE candidates ADD COLUMN IF NOT EXISTS last_activity_at timestamp;
